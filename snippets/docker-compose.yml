services:
  experiment:
    build: .
    runtime: nvidia
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              device_ids: ['0']
              capabilities: [gpu]
    environment:
      - BATCH_SIZE=32
    volumes:
      - ./data:/data:ro
      - ./results:/results