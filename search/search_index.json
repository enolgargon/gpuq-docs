{"config":{"lang":["es","en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Gesti\u00f3n de colas para GPU en experimentos - gpuq","text":"<p><code>gpuq</code> es una herramienta dise\u00f1ada para gestionar el uso compartido de GPUs en un servidor de investigaci\u00f3n.</p> <p>En muchos servidores utilizados por estudiantes y grupos de investigaci\u00f3n, varias personas ejecutan experimentos de machine learning al mismo tiempo. Esto suele provocar problemas como:</p> <ul> <li>varios usuarios intentando usar la misma GPU</li> <li>experimentos que fallan porque la GPU est\u00e1 ocupada</li> <li>dificultad para reproducir experimentos en distintos entornos</li> </ul> <p><code>gpuq</code> resuelve estos problemas utilizando:</p> <ul> <li>Docker para definir el entorno de ejecuci\u00f3n de los experimentos</li> <li>docker-compose para describir c\u00f3mo se ejecuta cada experimento</li> <li>una cola de trabajos que garantiza que solo un experimento utilice la GPU a la vez</li> </ul>"},{"location":"#que-aprenderas-en-esta-documentacion","title":"Qu\u00e9 aprender\u00e1s en esta documentaci\u00f3n","text":"<p>Esta documentaci\u00f3n explica paso a paso c\u00f3mo:</p> <ol> <li>Entender qu\u00e9 es Docker y por qu\u00e9 se utiliza en experimentaci\u00f3n</li> <li>Definir un experimento usando <code>Dockerfile</code> y <code>docker-compose</code></li> <li>Ejecutar experimentos en el servidor utilizando <code>gpuq</code></li> </ol> <p>No es necesario tener experiencia previa con Docker.</p>"},{"location":"#flujo-de-trabajo","title":"Flujo de trabajo","text":"<p>El flujo t\u00edpico de trabajo es el siguiente:</p> <ol> <li>Definir el entorno del experimento con un Dockerfile</li> <li>Definir c\u00f3mo se ejecuta el experimento con docker-compose</li> <li>Enviar el experimento a la cola utilizando gpuq</li> </ol>"},{"location":"#estructura-de-un-experimento","title":"Estructura de un experimento","text":"<p>En esta documentaci\u00f3n utilizaremos una estructura sencilla para los experimentos:</p> <pre><code>experimento/\n\u251c\u2500\u2500 Dockerfile\n\u251c\u2500\u2500 docker-compose.yml\n\u251c\u2500\u2500 train.py\n\u251c\u2500\u2500 requirements.txt\n\u251c\u2500\u2500 data/\n\u2514\u2500\u2500 results/\n</code></pre> <p>Donde:</p> <ul> <li>Dockerfile define el entorno de ejecuci\u00f3n</li> <li>docker-compose.yml define c\u00f3mo se ejecuta el experimento</li> <li>data/ contiene los datos de entrada</li> <li>results/ contiene los resultados generados</li> </ul>"},{"location":"docker/conceptos-basicos/","title":"Conceptos b\u00e1sicos de Docker","text":"<p>En esta secci\u00f3n se introducen algunos conceptos b\u00e1sicos de Docker que son necesarios para entender c\u00f3mo se ejecutan los experimentos en este servidor.</p> <p>No es necesario conocer Docker en profundidad. Solo veremos los conceptos m\u00ednimos que se utilizar\u00e1n en los ejemplos de esta documentaci\u00f3n.</p>"},{"location":"docker/conceptos-basicos/#imagen","title":"Imagen","text":"<p>Una imagen Docker es una plantilla que contiene todo lo necesario para ejecutar un programa.</p> <p>Una imagen puede incluir:</p> <ul> <li>un sistema operativo base</li> <li>librer\u00edas del sistema</li> <li>dependencias de Python</li> <li>herramientas necesarias para el experimento</li> </ul> <p>Las im\u00e1genes se utilizan como base para crear contenedores.</p>"},{"location":"docker/conceptos-basicos/#contenedor","title":"Contenedor","text":"<p>Un contenedor es una instancia en ejecuci\u00f3n de una imagen.</p> <p>Podemos pensar en la relaci\u00f3n entre im\u00e1genes y contenedores de forma similar a:</p> <ul> <li>imagen \u2192 plantilla</li> <li>contenedor \u2192 programa en ejecuci\u00f3n</li> </ul> <p>Cuando ejecutamos un experimento con Docker, en realidad lo que ocurre es que Docker crea un contenedor a partir de una imagen y ejecuta el programa dentro de \u00e9l.</p> <p>Cada contenedor est\u00e1 aislado del sistema y de otros contenedores.</p> <p>Esto permite que distintos experimentos utilicen dependencias distintas sin interferir entre s\u00ed.</p>"},{"location":"docker/conceptos-basicos/#dockerfile","title":"Dockerfile","text":"<p>Un Dockerfile es un fichero que describe c\u00f3mo construir una imagen Docker.</p> <p>En \u00e9l se definen aspectos como:</p> <ul> <li>la imagen base</li> <li>las dependencias que se deben instalar</li> <li>el directorio de trabajo</li> <li>la orden que ejecuta el experimento</li> </ul> <p>Un ejemplo simple de Dockerfile podr\u00eda ser:</p> <pre><code>FROM python:3.10\n\nWORKDIR /workspace\n\nCOPY requirements.txt .\nRUN pip install -r requirements.txt\n\nCOPY train.py .\n\nCMD [\"python\", \"train.py\"]\n</code></pre> <p>Este fichero indica a Docker c\u00f3mo construir la imagen que se utilizar\u00e1 para ejecutar el experimento.</p> <p>En la siguiente secci\u00f3n veremos con m\u00e1s detalle c\u00f3mo escribir un Dockerfile.</p>"},{"location":"docker/conceptos-basicos/#volumenes","title":"Vol\u00famenes","text":"<p>Los vol\u00famenes permiten compartir ficheros entre el sistema anfitri\u00f3n (el servidor) y el contenedor.</p> <p>Esto es especialmente \u00fatil para trabajar con:</p> <ul> <li>datasets</li> <li>resultados de experimentos</li> <li>ficheros de configuraci\u00f3n</li> </ul> <p>Por ejemplo, podemos montar un directorio del servidor dentro del contenedor: <code>./data \u2192 /data</code></p> <p>De esta forma el contenedor puede leer los datos desde /data y los resultados que escriba el contenedor permanecen en el sistema del servidor.</p> <p>Esto permite separar claramente:</p> <ul> <li>el entorno del experimento (dentro del contenedor)</li> <li>los datos y resultados (fuera del contenedor)</li> </ul>"},{"location":"docker/conceptos-basicos/#variables-de-entorno","title":"Variables de entorno","text":"<p>Las variables de entorno permiten pasar par\u00e1metros al experimento cuando se ejecuta el contenedor.</p> <p>Por ejemplo, podemos definir par\u00e1metros como:</p> <pre><code>EPOCHS=50\nLEARNING_RATE=0.001\n</code></pre> <p>El programa puede leer estas variables para configurar su comportamiento.</p> <p>Esto permite cambiar par\u00e1metros del experimento sin modificar el c\u00f3digo.</p>"},{"location":"docker/docker-compose/","title":"Usar docker-compose","text":"<p>Mientras que el Dockerfile define el entorno del experimento, el fichero docker-compose.yml define c\u00f3mo se ejecuta el experimento.</p> <p>En \u00e9l se especifican aspectos como:</p> <ul> <li>qu\u00e9 imagen se utilizar\u00e1</li> <li>qu\u00e9 variables de entorno se definen</li> <li>qu\u00e9 directorios se comparten con el contenedor</li> <li>si el experimento utiliza GPU</li> </ul> <p>Este fichero describe completamente c\u00f3mo debe ejecutarse el experimento.</p>"},{"location":"docker/docker-compose/#ejemplo-de-docker-compose","title":"Ejemplo de docker-compose","text":"<p>Un fichero <code>docker-compose.yml</code> t\u00edpico para un experimento puede tener el siguiente aspecto:</p> <pre><code>services:\n\n  experiment:\n\n    build: .\n\n    runtime: nvidia\n    deploy: \n      resources: \n        reservations: \n          devices: \n            - driver: nvidia \n              device_ids: ['0'] \n              capabilities: [gpu]\n\n    environment:\n      - EPOCHS=50\n      - LEARNING_RATE=0.001\n\n    volumes:\n      - ./data:/data:ro\n      - ./results:/results\n</code></pre> <p>Este fichero define el contenedor que ejecutar\u00e1 el experimento.</p>"},{"location":"docker/docker-compose/#construccion-de-la-imagen","title":"Construcci\u00f3n de la imagen","text":"<p>La opci\u00f3n build indica que Docker debe construir la imagen utilizando el Dockerfile del directorio actual.</p> <pre><code>build: .\n</code></pre> <p>Esto significa que antes de ejecutar el experimento, Docker construir\u00e1 la imagen definida por el Dockerfile.</p>"},{"location":"docker/docker-compose/#uso-de-gpu","title":"Uso de GPU","text":"<p>Para que el contenedor pueda utilizar la GPU del servidor se utiliza:</p> <pre><code>runtime: nvidia\ndeploy: \n  resources: \n    reservations: \n      devices: \n        - driver: nvidia \n          device_ids: ['0'] \n          capabilities: [gpu] \n</code></pre> <p>Esto permite que el contenedor acceda a CUDA y a la GPU disponible.</p>"},{"location":"docker/docker-compose/#variables-de-entorno","title":"Variables de entorno","text":"<p>Las variables de entorno permiten definir par\u00e1metros del experimento.</p> <pre><code>environment:\n  - EPOCHS=50\n  - LEARNING_RATE=0.001\n</code></pre> <p>Estas variables estar\u00e1n disponibles dentro del contenedor y el programa puede leerlas para configurar su comportamiento.</p> <p>Esto permite cambiar la configuraci\u00f3n del experimento sin modificar el c\u00f3digo ni reconstruir la imagen Docker.</p>"},{"location":"docker/docker-compose/#volumenes","title":"Vol\u00famenes","text":"<p>Los vol\u00famenes permiten compartir directorios entre el servidor y el contenedor.</p> <pre><code>volumes:\n  - ./data:/data:ro\n  - ./results:/results\n</code></pre> <p>En este ejemplo:</p> <ul> <li><code>./data</code> contiene los datos de entrada del experimento</li> <li><code>./results</code> se utiliza para guardar los resultados</li> </ul> <p>Observa que la definici\u00f3n del volumen de datos contiene al final la etiqueta <code>:ro</code>. Eso quiere decir que ese volumen se montar\u00e1 en modo s\u00f3lo lectura. El contenedor podr\u00e1 ver esos datos, los podr\u00e1 abrir y podr\u00e1 operar con ellos, pero no podr\u00e1 escribir en ese directorio. Eso protege los datos de ser modificados de forma accidentarl.</p> <p>En el caso del directorio de resultados, no se ha incluido esta configuraci\u00f3n. Por defecto, si no se indica, el directorio permitir\u00e1 tanto lectura como escritura.</p> <p>Dentro del contenedor estos directorios estar\u00e1n disponibles como:</p> <pre><code>/data\n/results\n</code></pre> <p>Esto permite que los resultados generados por el contenedor permanezcan en el sistema del servidor.</p>"},{"location":"docker/dockerfile/","title":"Crear un Dockerfile","text":"<p>Un Dockerfile es un fichero que describe c\u00f3mo construir la imagen Docker que se utilizar\u00e1 para ejecutar un experimento.</p> <p>En este fichero se define:</p> <ul> <li>la imagen base</li> <li>las dependencias necesarias</li> <li>el c\u00f3digo que se ejecutar\u00e1</li> <li>la orden que lanza el experimento</li> </ul> <p>A partir del Dockerfile, Docker construye una imagen que luego se utilizar\u00e1 para crear el contenedor donde se ejecutar\u00e1 el experimento.</p>"},{"location":"docker/dockerfile/#estructura-basica-de-un-dockerfile","title":"Estructura b\u00e1sica de un Dockerfile","text":"<p>Un Dockerfile t\u00edpico para un experimento puede tener una estructura como esta:</p> <pre><code>FROM pytorch/pytorch:2.2.0-cuda12.1-cudnn8-runtime\n\nWORKDIR /workspace\n\nCOPY requirements.txt .\nRUN pip install -r requirements.txt\n\nCOPY train.py .\n\nCMD [\"python\", \"train.py\"]\n</code></pre> <p>Este fichero define el entorno necesario para ejecutar el experimento.</p>"},{"location":"docker/dockerfile/#imagen-base","title":"Imagen base","text":"<p>La instrucci\u00f3n FROM indica qu\u00e9 imagen se utilizar\u00e1 como base.</p> <pre><code>FROM pytorch/pytorch:2.2.0-cuda12.1-cudnn8-runtime\n</code></pre> <p>Las im\u00e1genes base suelen incluir:</p> <ul> <li>Python</li> <li>bibliotecas de machine learning</li> </ul> <p>Elegir una buena imagen base permite evitar tener que instalar muchas dependencias manualmente.</p>"},{"location":"docker/dockerfile/#directorio-de-trabajo","title":"Directorio de trabajo","text":"<p>La instrucci\u00f3n WORKDIR define el directorio desde el que se ejecutar\u00e1n las \u00f3rdenes dentro del contenedor.</p> <pre><code>WORKDIR /workspace\n</code></pre> <p>A partir de este punto, los ficheros se copiar\u00e1n y las \u00f3rdenes se ejecutar\u00e1n en ese directorio.</p>"},{"location":"docker/dockerfile/#instalacion-de-dependencias","title":"Instalaci\u00f3n de dependencias","text":"<p>Normalmente las dependencias de Python se definen en un fichero requirements.txt.</p> <pre><code>COPY requirements.txt .\nRUN pip install -r requirements.txt\n</code></pre> <p>Primero se copia el fichero al contenedor y despu\u00e9s se instalan las dependencias.</p> <p>Esto permite que el entorno del experimento quede completamente definido.</p> <p>--</p>"},{"location":"docker/dockerfile/#copiar-el-codigo","title":"Copiar el c\u00f3digo","text":"<p>Despu\u00e9s se copia el c\u00f3digo del experimento.</p> <pre><code>COPY train.py .\n</code></pre> <p>Esto hace que el fichero est\u00e9 disponible dentro del contenedor.</p>"},{"location":"docker/dockerfile/#orden-de-ejecucion","title":"Orden de ejecuci\u00f3n","text":"<p>La instrucci\u00f3n CMD define qu\u00e9 orden se ejecutar\u00e1 cuando se inicie el contenedor.</p> <pre><code>CMD [\"python\", \"train.py\"]\n</code></pre> <p>En este caso, el contenedor ejecutar\u00e1 el script train.py.</p>"},{"location":"docker/dockerfile/#buenas-practicas","title":"Buenas pr\u00e1cticas","text":""},{"location":"docker/dockerfile/#evitar-copy","title":"Evitar COPY . .","text":"<p>Es habitual encontrar Dockerfiles que contienen:</p> <pre><code>COPY . .\n</code></pre> <p>Esto copia todo el directorio del proyecto dentro del contenedor.</p> <p>En experimentaci\u00f3n esto suele ser una mala pr\u00e1ctica porque puede incluir:</p> <ul> <li>datasets</li> <li>resultados de experimentos</li> <li>ficheros innecesarios</li> </ul> <p>Es preferible copiar solo los ficheros necesarios para ejecutar el experimento.</p>"},{"location":"docker/dockerfile/#separar-codigo-y-datos","title":"Separar c\u00f3digo y datos","text":"<p>Los datasets y resultados no deber\u00edan copiarse dentro de la imagen Docker.</p> <p>En su lugar, se utilizar\u00e1n vol\u00famenes para montar esos directorios cuando se ejecute el contenedor.</p> <p>Esto permite:</p> <ul> <li>reutilizar la misma imagen para distintos datasets</li> <li>evitar im\u00e1genes Docker muy grandes</li> <li>mantener separados c\u00f3digo y datos</li> </ul> <p>Veremos c\u00f3mo hacerlo en la secci\u00f3n de <code>docker-compose</code></p>"},{"location":"docker/dockerfile/#usar-variables-de-entorno-para-la-configuracion","title":"Usar variables de entorno para la configuraci\u00f3n","text":"<p>En muchos experimentos es necesario cambiar par\u00e1metros como:</p> <ul> <li>n\u00famero de \u00e9pocas</li> <li>tasa de aprendizaje</li> <li>tama\u00f1o de batch</li> <li>rutas de entrada o salida</li> </ul> <p>Una mala pr\u00e1ctica com\u00fan es modificar el c\u00f3digo cada vez que se quiere cambiar un par\u00e1metro y volver a construir la imagen Docker.</p> <p>Esto implica volver a ejecutar el proceso de construcci\u00f3n de la imagen, lo que puede ser lento e innecesario.</p> <p>En su lugar, es recomendable utilizar variables de entorno para definir estos par\u00e1metros.</p> <p>Por ejemplo, el c\u00f3digo del experimento puede leer valores como:</p> <ul> <li>EPOCHS</li> <li>LEARNING_RATE</li> <li>BATCH_SIZE</li> </ul> <p>Estas variables pueden definirse cuando se ejecuta el contenedor.</p> <p>Por ejemplo:</p> <pre><code>EPOCHS=50\nLEARNING_RATE=0.001\n</code></pre> <p>De esta forma:</p> <ul> <li>el c\u00f3digo del experimento no cambia</li> <li>no es necesario reconstruir la imagen</li> <li>se pueden lanzar m\u00faltiples experimentos con configuraciones distintas</li> </ul> <p>Esto permite reutilizar la misma imagen Docker para diferentes configuraciones del experimento.</p>"},{"location":"experimentos/ejecutar-contenedores/","title":"Ejecutar contenedores Docker","text":"<p>Una vez definidos el Dockerfile y el fichero docker-compose, el siguiente paso es ejecutar el experimento.</p> <p>En esta secci\u00f3n se describen las \u00f3rdenes b\u00e1sicas de Docker que se utilizar\u00e1n para ejecutar y gestionar contenedores en el servidor.</p> <p>No es necesario conocer Docker en profundidad. Solo veremos las \u00f3rdenes m\u00e1s habituales para trabajar con experimentos.</p>"},{"location":"experimentos/ejecutar-contenedores/#ejecutar-un-experimento","title":"Ejecutar un experimento","text":"<p>Para ejecutar un experimento definido con <code>docker-compose</code> se utiliza el orden:</p> <pre><code>docker compose up\n</code></pre> <p>Esta orden realiza varias acciones:</p> <ol> <li>Construye la imagen definida en el <code>Dockerfile</code> (si todav\u00eda no existe).</li> <li>Crea el contenedor.</li> <li>Ejecuta el experimento dentro del contenedor.</li> </ol> <p>Si el experimento ya se hab\u00eda ejecutado anteriormente y la imagen ya existe, Docker reutilizar\u00e1 esa imagen para lanzar el contenedor.</p>"},{"location":"experimentos/ejecutar-contenedores/#seleccionar-el-fichero-docker-compose","title":"Seleccionar el fichero docker-compose","text":"<p>Por defecto Docker busca un fichero llamado: <code>docker-compose.yml</code>.</p> <p>Si el proyecto utiliza ese nombre no es necesario especificar nada m\u00e1s.</p> <p>Sin embargo, si el proyecto contiene varios ficheros <code>docker-compose</code>, es posible seleccionar uno concreto utilizando la opci\u00f3n <code>-f</code>.</p> <p>Por ejemplo:</p> <pre><code>docker compose -f docker-compose.test.yml up\n</code></pre> <p>Esto permite tener varias configuraciones de ejecuci\u00f3n dentro del mismo proyecto, por ejemplo:</p> <ul> <li>una para ejecutar el experimento completo</li> <li>otra para ejecutar pruebas o experimentos r\u00e1pidos</li> </ul>"},{"location":"experimentos/ejecutar-contenedores/#ejecutar-en-segundo-plano","title":"Ejecutar en segundo plano","text":"<p>Muchos experimentos pueden tardar horas o incluso d\u00edas.</p> <p>Para evitar mantener la terminal abierta durante todo ese tiempo se puede utilizar la opci\u00f3n <code>-d</code> (detached mode):</p> <pre><code>docker compose up -d\n</code></pre> <p>o, si se quiere especificar el fichero:</p> <pre><code>docker compose -f docker-compose.yml up -d\n</code></pre> <p>En este modo el contenedor se ejecuta en segundo plano y es posible cerrar la sesi\u00f3n del terminal y volver m\u00e1s tarde.</p>"},{"location":"experimentos/ejecutar-contenedores/#detener-un-experimento","title":"Detener un experimento","text":"<p>Para detener los contenedores creados por <code>docker-compose</code> se utiliza:</p> <pre><code>docker compose down\n</code></pre> <p>Esta orden:</p> <ul> <li>detiene los contenedores</li> <li>elimina los contenedores creados por <code>docker-compose</code></li> </ul> <p>Es importante tener en cuenta que la imagen Docker no se elimina, solo el contenedor.</p> <p>Esto significa que el experimento puede volver a ejecutarse r\u00e1pidamente sin tener que reconstruir la imagen.</p>"},{"location":"experimentos/ejecutar-contenedores/#ver-contenedores","title":"Ver contenedores","text":"<p>Para ver los contenedores que est\u00e1n ejecut\u00e1ndose se utiliza:</p> <pre><code>docker container ls\n</code></pre> <p>Esta orden muestra informaci\u00f3n como:</p> <ul> <li>identificador del contenedor</li> <li>imagen utilizada</li> <li>estado</li> <li>nombre del contenedor</li> <li>tiempo de ejecuci\u00f3n</li> </ul>"},{"location":"experimentos/ejecutar-contenedores/#ver-todos-los-contenedores","title":"Ver todos los contenedores","text":"<p>Por defecto solo se muestran los contenedores que est\u00e1n en ejecuci\u00f3n.</p> <p>Para ver todos los contenedores, incluidos los que ya han terminado, se utiliza la opci\u00f3n <code>-a</code>:</p> <pre><code>docker container ls -a\n</code></pre> <p>Esto permite comprobar si un experimento ya ha terminado o si ha finalizado con alg\u00fan error.</p>"},{"location":"experimentos/ejecutar-contenedores/#ver-el-ultimo-contenedor","title":"Ver el \u00faltimo contenedor","text":"<p>La opci\u00f3n <code>-l</code> permite mostrar el \u00faltimo contenedor creado:</p> <pre><code>docker container ls -l\n</code></pre> <p>Esto es \u00fatil cuando se acaba de ejecutar un experimento y se quiere localizar r\u00e1pidamente el contenedor correspondiente.</p>"},{"location":"experimentos/ejecutar-contenedores/#ver-los-logs-de-un-contenedor","title":"Ver los logs de un contenedor","text":"<p>Para consultar la salida de un contenedor se utiliza:</p> <pre><code>docker logs &lt;container_id&gt;\n</code></pre> <p>Esta orden muestra la salida est\u00e1ndar del programa que se est\u00e1 ejecutando dentro del contenedor.</p> <p>En experimentos de machine learning esto suele incluir:</p> <ul> <li>progreso del entrenamiento</li> <li>m\u00e9tricas</li> <li>mensajes de error</li> <li>informaci\u00f3n de depuraci\u00f3n</li> </ul>"},{"location":"experimentos/ejecutar-contenedores/#identificador-del-contenedor","title":"Identificador del contenedor","text":"<p>El identificador del contenedor suele ser una cadena larga, por ejemplo <code>8f3a1c2b4d9e7c...</code>. No es necesario escribir el identificador completo.</p> <p>Docker permite utilizar solo los primeros caracteres, siempre que sean suficientes para diferenciar ese contenedor de los dem\u00e1s.</p> <p>Por ejemplo:</p> <pre><code>docker logs 8f3a\n</code></pre> <p>Funcionar\u00e1 siempre que no exista otro contenedor cuyo identificador empiece por <code>8f3a</code>.</p>"},{"location":"experimentos/ejecutar-contenedores/#gestionar-imagenes-docker","title":"Gestionar im\u00e1genes Docker","text":"<p>Las im\u00e1genes Docker que se han construido en el sistema pueden listarse con:</p> <pre><code>docker image ls\n</code></pre> <p>Esta orden muestra:</p> <ul> <li>nombre de la imagen</li> <li>identificador</li> <li>fecha de creaci\u00f3n</li> <li>tama\u00f1o</li> </ul> <p>Las im\u00e1genes contienen el entorno completo necesario para ejecutar el experimento.</p>"},{"location":"experimentos/ejecutar-contenedores/#eliminar-una-imagen","title":"Eliminar una imagen","text":"<p>Para eliminar una imagen se utiliza:</p> <pre><code>docker image rm &lt;image_id&gt;\n</code></pre> <p>Eliminar una imagen es importante y necesario cuando se realizan cambios en el c\u00f3digo o en el entorno del experimento.</p> <p>Si una imagen ya existe, <code>docker compose</code> la reutilizar\u00e1 en ejecuciones posteriores y no reconstruir\u00e1 la imagen autom\u00e1ticamente.</p> <p>Esto significa que los cambios realizados en el c\u00f3digo podr\u00edan no reflejarse en el contenedor.</p> <p>Eliminar la imagen obliga a Docker a reconstruirla la pr\u00f3xima vez que se ejecute el experimento.</p>"},{"location":"experimentos/ejecutar-contenedores/#resumen","title":"Resumen","text":"<p>Las \u00f3rdenes m\u00e1s utilizadas para trabajar con contenedores son:</p> Orden Descripci\u00f3n <code>docker compose up -d</code> Ejecutar un experimento en segundo plano <code>docker compose down</code> Detener y eliminar los contenedores <code>docker container ls</code> Ver contenedores en ejecuci\u00f3n <code>docker container ls -a</code> Ver todos los contenedores <code>docker container ls -l</code> Ver el \u00faltimo contenedor <code>docker logs &lt;container&gt;</code> Ver la salida de un contenedor <code>docker image ls</code> Listar im\u00e1genes disponibles <code>docker image rm &lt;image&gt;</code> Eliminar una imagen"},{"location":"experimentos/ejemplo-minimo/","title":"Ejemplo m\u00ednimo","text":"<p>En esta secci\u00f3n se muestra un ejemplo m\u00ednimo de c\u00f3mo ejecutar un experimento utilizando los ficheros descritos en las secciones anteriores.</p> <p>Un proyecto t\u00edpico puede tener una estructura como la siguiente:</p> <pre><code>experiment/\n\u251c\u2500\u2500 Dockerfile\n\u251c\u2500\u2500 docker-compose.yml\n\u251c\u2500\u2500 docker-compose.test.yml\n\u251c\u2500\u2500 requirements.txt\n\u251c\u2500\u2500 main.py\n\u251c\u2500\u2500 model.py\n\u251c\u2500\u2500 test.py\n\u251c\u2500\u2500 data/\n\u2514\u2500\u2500 results/\n</code></pre> <p>El experimento puede ejecutarse de dos formas distintas dependiendo de si necesita GPU o no.</p> <ul> <li>Experimentos sin GPU \u2192 ejecutar directamente con <code>docker compose</code></li> <li>Experimentos con GPU \u2192 enviar a la cola con <code>gpuq</code></li> </ul>"},{"location":"experimentos/ejemplo-minimo/#ejecutar-un-experimento-sin-gpu","title":"Ejecutar un experimento sin GPU","text":"<p>Si el experimento no necesita GPU, puede ejecutarse directamente con Docker.</p> <p>Por ejemplo, para ejecutar el experimento definido en <code>docker-compose.yml</code>:</p> <pre><code>docker compose up -d\n</code></pre> <p>Esta orden:</p> <ol> <li>construye la imagen definida en el <code>Dockerfile</code> (si no existe)</li> <li>crea el contenedor</li> <li>ejecuta el experimento en segundo plano</li> </ol>"},{"location":"experimentos/ejemplo-minimo/#ejecutar-un-experimento-con-gpu","title":"Ejecutar un experimento con GPU","text":"<p>Los experimentos que utilizan GPU no deben ejecutarse directamente con Docker.</p> <p>En su lugar deben enviarse a la cola utilizando gpuq.</p> <p>Esto garantiza que:</p> <ul> <li>solo un experimento utilice la GPU a la vez</li> <li>los experimentos se ejecuten en orden</li> <li>se mantenga un registro persistente de los trabajos ejecutados</li> </ul>"},{"location":"experimentos/ejemplo-minimo/#enviar-un-trabajo-a-la-cola","title":"Enviar un trabajo a la cola","text":"<p>Para enviar un experimento a la cola se utiliza la orden:</p> <pre><code>gpuq submit --project PATH\n</code></pre> <p>donde <code>PATH</code> es el directorio que contiene el proyecto.</p> <p>Por ejemplo:</p> <pre><code>gpuq submit --project ./experiment-name\n</code></pre> <p>Esta orden:</p> <ul> <li>registra el trabajo en la cola</li> <li>genera un identificador \u00fanico para el trabajo</li> <li>asocia el trabajo con el usuario que lo ha enviado</li> </ul> <p>El trabajo queda en estado queued hasta que el sistema lo ejecute.</p>"},{"location":"experimentos/ejemplo-minimo/#seleccionar-el-fichero-docker-compose","title":"Seleccionar el fichero docker-compose","text":"<p>Por defecto <code>gpuq</code> utilizar\u00e1 el fichero <code>docker-compose.yml</code>.  Si se desea utilizar otro fichero <code>docker-compose</code>, se puede indicar con la opci\u00f3n <code>--compose</code>.</p> <p>Por ejemplo:</p> <pre><code>gpuq submit --project ./experiment --compose docker-compose.test.yml\n</code></pre>"},{"location":"experimentos/ejemplo-minimo/#listar-trabajos-en-la-cola","title":"Listar trabajos en la cola","text":"<p>Para ver los trabajos registrados en la cola se utiliza:</p> <pre><code>gpuq list\n</code></pre> <p>Esta orden muestra informaci\u00f3n como:</p> <ul> <li>identificador del trabajo</li> <li>usuario que lo envi\u00f3</li> <li>fecha de creaci\u00f3n</li> <li>descripci\u00f3n</li> </ul>"},{"location":"experimentos/ejemplo-minimo/#cancelar-un-trabajo","title":"Cancelar un trabajo","text":"<p>Para cancelar un trabajo se utiliza:</p> <pre><code>gpuq cancel JOB_ID\n</code></pre> <p>Por ejemplo:</p> <pre><code>gpuq cancel job-1a2b3c4d\n</code></pre> <p>Si el trabajo est\u00e1 en estado queued, se mover\u00e1 al estado canceled.</p> <p>Si el trabajo est\u00e1 en estado running, el sistema marcar\u00e1 el trabajo como cancelado y el proceso de ejecuci\u00f3n se encargar\u00e1 de detenerlo.</p>"},{"location":"experimentos/estructura-experimento/","title":"Estructura de un experimento","text":"<p>En esta secci\u00f3n se describe una estructura sencilla y recomendada para organizar un experimento que se vaya a ejecutar en el servidor.</p> <p>La idea es separar claramente tres elementos distintos:</p> <ul> <li>el entorno de ejecuci\u00f3n</li> <li>el c\u00f3digo del experimento</li> <li>los datos y resultados</li> </ul> <p>Esta separaci\u00f3n facilita:</p> <ul> <li>reproducir experimentos</li> <li>cambiar configuraciones f\u00e1cilmente</li> <li>evitar reconstrucciones innecesarias de im\u00e1genes Docker</li> </ul>"},{"location":"experimentos/estructura-experimento/#estructura-recomendada","title":"Estructura recomendada","text":"<p>Un experimento t\u00edpico puede tener una estructura como la siguiente:</p> <pre><code>experiment/\n\u251c\u2500\u2500 Dockerfile\n\u251c\u2500\u2500 docker-compose.yml\n\u251c\u2500\u2500 docker-compose.test.yml\n\u251c\u2500\u2500 requirements.txt\n\u251c\u2500\u2500 main.py\n\u251c\u2500\u2500 model.py\n\u251c\u2500\u2500 test.py\n\u251c\u2500\u2500 data/\n\u2514\u2500\u2500 results/\n</code></pre> <p>Donde:</p> Fichero o directorio Descripci\u00f3n <code>Dockerfile</code> Define el entorno de ejecuci\u00f3n del experimento <code>docker-compose.yml</code> Define c\u00f3mo se ejecuta el experimento principal <code>docker-compose.test.yml</code> Variante para ejecutar pruebas <code>requirements.txt</code> Dependencias de Python <code>main.py</code> Script principal del experimento <code>model.py</code> Definici\u00f3n del modelo <code>test.py</code> Script para pruebas o experimentos r\u00e1pidos <code>data/</code> Datos de entrada <code>results/</code> Resultados generados por el experimento"},{"location":"experimentos/estructura-experimento/#dockerfile","title":"Dockerfile","text":"<p>El Dockerfile define el entorno en el que se ejecutar\u00e1 el experimento.</p> <p>Esto incluye:</p> <ul> <li>la imagen base</li> <li>las dependencias</li> <li>los ficheros necesarios</li> <li>la orden por defecto</li> </ul> <p> Descargar Dockerfile</p> <pre><code>FROM python:3.11\nENV PYTHONUNBUFFERED=1\n\nWORKDIR /workspace\n\nCOPY requirements.txt .\nRUN pip install --no-cache-dir -r requirements.txt\n\nCOPY main.py .\nCOPY model.py .\nCOPY test.py .\n\nCMD [\"python\", \"main.py\"]\n</code></pre> <p>Este fichero se utiliza para construir la imagen Docker que ejecutar\u00e1 el experimento.</p>"},{"location":"experimentos/estructura-experimento/#docker-compose","title":"docker-compose","text":"<p>El fichero docker-compose define c\u00f3mo se ejecuta el experimento.</p> <p>En \u00e9l se especifican aspectos como:</p> <ul> <li>c\u00f3mo construir la imagen</li> <li>qu\u00e9 variables de entorno utilizar</li> <li>qu\u00e9 directorios montar como vol\u00famenes</li> <li>si el experimento utiliza GPU</li> </ul>"},{"location":"experimentos/estructura-experimento/#ejecucion-del-experimento-principal","title":"Ejecuci\u00f3n del experimento principal","text":"<p>Este fichero ejecuta el experimento principal utilizando main.py.</p> <p> Descargar docker-compose.yml</p> <pre><code>services:\n  experiment:\n    build: .\n    runtime: nvidia\n    deploy:\n      resources:\n        reservations:\n          devices:\n            - driver: nvidia\n              device_ids: ['0']\n              capabilities: [gpu]\n    environment:\n      - BATCH_SIZE=32\n    volumes:\n      - ./data:/data:ro\n      - ./results:/results\n</code></pre>"},{"location":"experimentos/estructura-experimento/#ejecucion-de-pruebas","title":"Ejecuci\u00f3n de pruebas","text":"<p>A veces es \u00fatil ejecutar una variante del experimento, por ejemplo para:</p> <ul> <li>probar cambios r\u00e1pidamente</li> <li>ejecutar tests</li> <li>validar que el entorno funciona correctamente</li> </ul> <p>Para ello podemos crear un segundo fichero docker-compose.</p> <p> Descargar docker-compose.test.yml</p> <pre><code>services:\n  experiment:\n    build: .\n    runtime: nvidia\n    deploy:\n      resources:\n        reservations:\n          devices:\n            - driver: nvidia\n              device_ids: ['0']\n              capabilities: [gpu]\n    environment:\n      - EPOCHS=50\n      - LEARNING_RATE=0.001\n      - BATCH_SIZE=32\n    volumes:\n      - ./data:/data:ro\n      - ./results:/results\n    command: [\"python\", \"test.py\"]\n</code></pre> <p>Este fichero puede modificar aspectos como:</p> <ul> <li>el script que se ejecuta. IMPORTANTE: Observa como se utiliza el campo <code>command</code> para cambiar el script que se ejecuta al lanzar el experimento </li> <li>los par\u00e1metros del experimento</li> <li>configuraciones espec\u00edficas para pruebas</li> </ul>"},{"location":"gpuq/ejecutar-trabajos/","title":"Ejecutar trabajos con gpuq","text":"<p><code>gpuq</code> es una herramienta dise\u00f1ada para enviar experimentos que utilizan GPU a una cola de ejecuci\u00f3n en un servidor compartido.</p> <p>En lugar de ejecutar directamente un contenedor con Docker, los experimentos que requieren GPU deben enviarse a la cola mediante <code>gpuq</code>. Esto permite controlar el uso de la GPU y evitar que varios experimentos intenten utilizarla al mismo tiempo.</p> <p>Cada trabajo enviado a la cola queda registrado de forma persistente en el sistema de ficheros.</p>"},{"location":"gpuq/ejecutar-trabajos/#enviar-un-trabajo-a-la-cola","title":"Enviar un trabajo a la cola","text":"<p>Para enviar un experimento a la cola se utiliza la orden:</p> <pre><code>gpuq submit --project PATH\n</code></pre> <p>donde <code>PATH</code> es el directorio que contiene el proyecto.</p> <p>Por ejemplo:</p> <pre><code>gpuq submit --project ./experiment\n</code></pre> <p>El directorio del proyecto debe contener al menos un fichero <code>docker-compose</code> que describa c\u00f3mo ejecutar el experimento.</p> <p>Cuando se ejecuta esta orden:</p> <ul> <li>se genera un identificador \u00fanico para el trabajo</li> <li>el trabajo se asocia al usuario que lo ha enviado</li> <li>se registra la fecha de creaci\u00f3n</li> <li>el trabajo se guarda en la cola con estado queued</li> </ul> <p>El trabajo permanecer\u00e1 en ese estado hasta que el sistema encargado de ejecutar la cola lo inicie.</p>"},{"location":"gpuq/ejecutar-trabajos/#seleccionar-el-fichero-docker-compose","title":"Seleccionar el fichero docker-compose","text":"<p>Por defecto <code>gpuq</code> utilizar\u00e1 el fichero <code>docker-compose.yml</code>.  Si el proyecto contiene varios ficheros <code>docker-compose</code>, es posible seleccionar uno concreto utilizando la opci\u00f3n <code>--compose</code>.</p> <p>Por ejemplo:</p> <pre><code>gpuq submit --project ./experiment --compose docker-compose.test.yml\n</code></pre> <p>Esto permite utilizar diferentes configuraciones dentro del mismo proyecto.</p> <p>Por ejemplo:</p> <ul> <li>ejecutar el experimento principal</li> <li>ejecutar pruebas r\u00e1pidas</li> <li>ejecutar configuraciones alternativas</li> </ul>"},{"location":"gpuq/ejecutar-trabajos/#anadir-una-descripcion-al-trabajo","title":"A\u00f1adir una descripci\u00f3n al trabajo","text":"<p>Es posible a\u00f1adir una descripci\u00f3n opcional al trabajo utilizando la opci\u00f3n <code>--description</code>.</p> <p>Por ejemplo:</p> <pre><code>gpuq submit --project ./experiment --description \"baseline training\"\n</code></pre> <p>La descripci\u00f3n es un texto libre que permite identificar el prop\u00f3sito del experimento.</p>"},{"location":"gpuq/ejecutar-trabajos/#que-ocurre-al-enviar-un-trabajo","title":"Qu\u00e9 ocurre al enviar un trabajo","text":"<p>Cuando se ejecuta <code>gpuq submit</code>, la herramienta no ejecuta el experimento directamente.</p> <p>En su lugar:</p> <ol> <li>valida que el directorio del proyecto existe</li> <li>registra el trabajo en la cola</li> <li>asigna un identificador \u00fanico al trabajo</li> <li>guarda los metadatos del trabajo en el sistema de ficheros</li> </ol> <p>El trabajo queda registrado con estado <code>queued</code>. Un componente independiente del sistema se encargar\u00e1 posteriormente de ejecutar el trabajo.</p>"},{"location":"gpuq/ejecutar-trabajos/#identificador-del-trabajo","title":"Identificador del trabajo","text":"<p>Cada trabajo recibe un identificador \u00fanico con un formato similar a <code>job-1a2b3c4d</code>. Este identificador se utiliza para:</p> <ul> <li>consultar el estado del trabajo</li> <li>cancelar el trabajo</li> <li>identificar el experimento en la cola</li> </ul>"},{"location":"gpuq/ejecutar-trabajos/#que-hace-gpuq-y-que-no-hace","title":"Qu\u00e9 hace gpuq y qu\u00e9 no hace","text":"<p>Es importante entender qu\u00e9 responsabilidades tiene <code>gpuq</code> y cu\u00e1les no.</p> <p><code>gpuq</code> se encarga de:</p> <ul> <li>registrar trabajos en una cola persistente</li> <li>asignar identificadores \u00fanicos a los trabajos</li> <li>mantener el estado de cada trabajo</li> <li>permitir listar y cancelar trabajos</li> </ul> <p>Sin embargo, <code>gpuq</code> no ejecuta directamente los contenedores. La ejecuci\u00f3n real es realizada por un componente independiente del sistema.</p> <p>Adem\u00e1s, <code>gpuq</code> no elimina contenedores ni im\u00e1genes Docker.</p> <p>Cuando un trabajo termina, los contenedores creados por Docker permanecen en el sistema. Del mismo modo, las im\u00e1genes Docker construidas para ejecutar los experimentos tampoco se eliminan autom\u00e1ticamente.</p> <p>La limpieza de contenedores o im\u00e1genes que ya no sean necesarios sigue siendo responsabilidad del usuario.</p> <p>Las \u00f3rdenes necesarias para gestionar contenedores e im\u00e1genes se describen en la secci\u00f3n sobre uso de Docker.</p>"},{"location":"gpuq/ejecutar-trabajos/#resumen","title":"Resumen","text":"<p>Las \u00f3rdenes principales para enviar trabajos a la cola con gpuq son los siguientes:</p> Orden Descripci\u00f3n <code>gpuq submit --project ./experiment</code> Env\u00eda un experimento a la cola utilizando el fichero <code>docker-compose.yml</code> del proyecto. <code>gpuq submit --project ./experiment --compose docker-compose.test.yml</code> Env\u00eda el experimento utilizando un fichero <code>docker-compose</code> espec\u00edfico. <code>gpuq submit --project ./experiment --description \"baseline training\"</code> Env\u00eda un experimento a\u00f1adiendo una descripci\u00f3n para identificar el trabajo. <p>En todos los casos la orden registra el trabajo en la cola, genera un identificador \u00fanico y deja el trabajo en estado queued hasta que el sistema encargado de ejecutar la cola lo inicie.</p> <p>En la siguiente secci\u00f3n veremos c\u00f3mo monitorizar los trabajos en la cola y consultar su estado.</p>"},{"location":"gpuq/logs-y-resultados/","title":"Logs y resultados","text":"<p>Una vez que un trabajo ha sido enviado a la cola y comienza su ejecuci\u00f3n, es habitual querer consultar:</p> <ul> <li>la salida del experimento</li> <li>posibles errores durante la ejecuci\u00f3n</li> <li>los resultados generados</li> </ul> <p>En esta secci\u00f3n se describe c\u00f3mo acceder a esta informaci\u00f3n.</p>"},{"location":"gpuq/logs-y-resultados/#salida-del-experimento","title":"Salida del experimento","text":"<p>Los experimentos enviados mediante <code>gpuq</code> se ejecutan dentro de contenedores Docker utilizando la configuraci\u00f3n definida en el fichero <code>docker-compose</code>.</p> <p>Por este motivo, la salida del experimento se puede consultar utilizando las herramientas est\u00e1ndar de Docker.</p>"},{"location":"gpuq/logs-y-resultados/#ver-los-logs-de-un-contenedor","title":"Ver los logs de un contenedor","text":"<p>Para consultar la salida de un contenedor se utiliza la orden:</p> <pre><code>docker logs CONTAINER_ID\n</code></pre> <p>Esta orden muestra la salida est\u00e1ndar del programa que se est\u00e1 ejecutando dentro del contenedor.</p> <p>En experimentos de machine learning esto suele incluir:</p> <ul> <li>progreso del entrenamiento</li> <li>m\u00e9tricas del modelo</li> <li>mensajes de depuraci\u00f3n</li> <li>errores de ejecuci\u00f3n</li> </ul>"},{"location":"gpuq/logs-y-resultados/#localizar-el-contenedor","title":"Localizar el contenedor","text":"<p>Para encontrar el contenedor asociado a un experimento se puede utilizar:</p> <pre><code>docker container ls -a\n</code></pre>"},{"location":"gpuq/logs-y-resultados/#resultados-del-experimento","title":"Resultados del experimento","text":"<p>En la estructura recomendada para los proyectos se utiliza un directorio llamado <code>results</code>. Este directorio se monta dentro del contenedor mediante un volumen definido en <code>docker-compose</code>.</p> <p>Esto significa que cualquier fichero que el experimento escriba en <code>/results</code> dentro del contenedor aparecer\u00e1 autom\u00e1ticamente en el directorio <code>results/</code> del proyecto.</p> <p>De esta forma:</p> <ul> <li>los resultados permanecen en el sistema incluso cuando el contenedor termina</li> <li>es posible analizar los resultados sin necesidad de acceder al contenedor</li> </ul>"},{"location":"gpuq/logs-y-resultados/#depuracion-de-errores","title":"Depuraci\u00f3n de errores","text":"<p>Si un experimento falla, los pasos habituales para investigar el problema son:</p> <ol> <li>consultar el estado del trabajo con <code>gpuq list</code></li> <li>localizar el contenedor correspondiente</li> <li>revisar los logs del contenedor con <code>docker logs</code></li> </ol> <p>Los mensajes de error del programa suelen aparecer en los logs del contenedor.</p>"},{"location":"gpuq/logs-y-resultados/#limpieza-de-contenedores-e-imagenes","title":"Limpieza de contenedores e im\u00e1genes","text":"<p>Es importante recordar que <code>gpuq</code> no elimina contenedores ni im\u00e1genes Docker.</p> <p>Cuando un experimento termina:</p> <ul> <li>el contenedor creado por Docker permanece en el sistema</li> <li>la imagen Docker utilizada para ejecutarlo tambi\u00e9n permanece</li> </ul> <p>La eliminaci\u00f3n de contenedores o im\u00e1genes que ya no sean necesarios es responsabilidad del usuario.</p> <p>Las \u00f3rdenes necesarias para gestionar estos recursos se describen en la secci\u00f3n sobre uso de Docker.</p>"},{"location":"gpuq/logs-y-resultados/#resumen","title":"Resumen","text":"<p>Las herramientas principales para consultar la ejecuci\u00f3n de un experimento son:</p> Orden Descripci\u00f3n <code>gpuq list</code> Mostrar el estado de los trabajos en la cola <code>docker container ls</code> Mostrar contenedores en ejecuci\u00f3n <code>docker container ls -a</code> Mostrar todos los contenedores <code>docker logs CONTAINER_ID</code> Consultar la salida de un contenedor <p>Los resultados del experimento se almacenan en el directorio <code>results/</code> del proyecto.</p>"},{"location":"gpuq/monitorizar-trabajos/","title":"Monitorizar trabajos","text":"<p>Una vez que un trabajo ha sido enviado a la cola con <code>gpuq</code>, es posible consultar su estado y gestionar su ejecuci\u00f3n utilizando las \u00f3rdenes de monitorizaci\u00f3n.</p> <p>En esta secci\u00f3n se describen las \u00f3rdenes disponibles para:</p> <ul> <li>listar trabajos en la cola</li> <li>consultar su estado</li> <li>cancelar trabajos si es necesario</li> </ul>"},{"location":"gpuq/monitorizar-trabajos/#listar-trabajos","title":"Listar trabajos","text":"<p>Para ver los trabajos registrados en la cola se utiliza:</p> <pre><code>gpuq list\n</code></pre> <p>Esta orden muestra todos los trabajos conocidos por la cola, independientemente de su estado.</p> <p>Para cada trabajo se muestra informaci\u00f3n como:</p> <ul> <li>identificador del trabajo</li> <li>usuario que lo ha enviado</li> <li>fecha de creaci\u00f3n</li> <li>descripci\u00f3n (si existe)</li> </ul> <p>Esta orden no modifica el estado del sistema, \u00fanicamente consulta la informaci\u00f3n almacenada en la cola.</p>"},{"location":"gpuq/monitorizar-trabajos/#estados-de-los-trabajos","title":"Estados de los trabajos","text":"<p>Cada trabajo en la cola se encuentra siempre en uno de los siguientes estados:</p> Estado Descripci\u00f3n <code>queued</code> El trabajo est\u00e1 registrado en la cola y espera a ser ejecutado. <code>running</code> El trabajo est\u00e1 actualmente en ejecuci\u00f3n. <code>finished</code> El trabajo ha finalizado correctamente. <code>failed</code> El trabajo ha terminado con un error. <code>canceled</code> El trabajo ha sido cancelado por el usuario. <p>El estado de un trabajo se determina por la ubicaci\u00f3n de su fichero dentro de la estructura de la cola en el sistema de ficheros.</p>"},{"location":"gpuq/monitorizar-trabajos/#filtrar-trabajos-por-estado","title":"Filtrar trabajos por estado","text":"<p>Es posible mostrar \u00fanicamente los trabajos que se encuentren en un estado concreto utilizando la opci\u00f3n <code>--state</code>.</p> <p>Por ejemplo, para ver \u00fanicamente los trabajos en espera:</p> <pre><code>gpuq list --state queued\n</code></pre> <p>Tambi\u00e9n se pueden consultar otros estados, por ejemplo:</p> <pre><code>gpuq list --state running\n</code></pre> <p>Esto resulta \u00fatil cuando hay muchos trabajos registrados en el sistema.</p>"},{"location":"gpuq/monitorizar-trabajos/#cancelar-un-trabajo","title":"Cancelar un trabajo","text":"<p>Para cancelar un trabajo se utiliza la orden:</p> <pre><code>gpuq cancel JOB_ID\n</code></pre> <p>Por ejemplo:</p> <pre><code>gpuq cancel job-1a2b3c4d\n</code></pre>"},{"location":"gpuq/monitorizar-trabajos/#cancelar-trabajos-en-estado-queued","title":"Cancelar trabajos en estado queued","text":"<p>Si el trabajo todav\u00eda est\u00e1 en estado <code>queued</code>, la orden lo mover\u00e1 al estado <code>canceled</code>. Esto significa que el trabajo no ser\u00e1 ejecutado.</p>"},{"location":"gpuq/monitorizar-trabajos/#cancelar-trabajos-en-estado-running","title":"Cancelar trabajos en estado running","text":"<p>Si el trabajo ya est\u00e1 en ejecuci\u00f3n (<code>running</code>), la orden marcar\u00e1 el trabajo como cancelado.</p> <p>La finalizaci\u00f3n real del proceso de ejecuci\u00f3n ser\u00e1 gestionada por el componente del sistema encargado de ejecutar los trabajos.</p>"},{"location":"gpuq/monitorizar-trabajos/#cancelar-trabajos-finalizados","title":"Cancelar trabajos finalizados","text":"<p>Intentar cancelar un trabajo que ya est\u00e9 en estado:</p> <ul> <li><code>finished</code></li> <li><code>failed</code></li> <li><code>canceled</code></li> </ul> <p>produce un error, ya que esos trabajos ya no pueden modificarse.</p>"},{"location":"gpuq/monitorizar-trabajos/#ciclo-de-vida-de-un-trabajo","title":"Ciclo de vida de un trabajo","text":"<p>Un trabajo enviado a la cola suele seguir el siguiente ciclo de vida:</p> <pre><code>stateDiagram-v2\n    [*] --&gt; queued : submit\n    queued --&gt; running : dispatcher starts job\n    queued --&gt; canceled : cancel\n    running --&gt; finished : success\n    running --&gt; failed : error\n    running --&gt; canceled : cancel\n</code></pre> <p>Este ciclo refleja las posibles transiciones de estado durante la ejecuci\u00f3n de un experimento.</p>"},{"location":"gpuq/monitorizar-trabajos/#resumen","title":"Resumen","text":"<p>Las \u00f3rdenes principales para monitorizar trabajos son los siguientes:</p> Orden Descripci\u00f3n <code>gpuq list</code> Muestra todos los trabajos registrados en la cola. <code>gpuq list --state queued</code> Muestra \u00fanicamente los trabajos que est\u00e1n en espera. <code>gpuq list --state running</code> Muestra los trabajos actualmente en ejecuci\u00f3n. <code>gpuq cancel JOB_ID</code> Cancela un trabajo identificado por su ID. <p>En la siguiente secci\u00f3n veremos c\u00f3mo consultar los logs de los experimentos y c\u00f3mo acceder a los resultados generados por los trabajos.</p>"},{"location":"introduccion/que-es-docker/","title":"\u00bfQu\u00e9 es Docker?","text":"<p>Docker es una herramienta que permite ejecutar programas dentro de entornos aislados llamados contenedores.</p> <p>Un contenedor incluye todo lo necesario para ejecutar un programa:</p> <ul> <li>el sistema operativo base</li> <li>las librer\u00edas necesarias</li> <li>las dependencias de Python</li> <li>el propio c\u00f3digo del experimento</li> </ul> <p>Esto permite ejecutar un experimento siempre en el mismo entorno, independientemente del sistema donde se ejecute.</p>"},{"location":"introduccion/que-es-docker/#el-problema-que-intenta-resolver","title":"El problema que intenta resolver","text":"<p>En experimentaci\u00f3n con machine learning es muy com\u00fan encontrarse con problemas como estos:</p> <ul> <li>un experimento funciona en tu ordenador pero falla en el servidor</li> <li>distintas versiones de Python o de una librer\u00eda provocan errores</li> <li>instalar dependencias rompe otros proyectos</li> <li>diferentes usuarios necesitan entornos distintos</li> </ul> <p>Por ejemplo, un experimento puede requerir python 3.10 y pytorch 2.1, mientras que otro puede necesitar python 3.8 y tensorflow 2.10.</p> <p>Instalar todo esto en el mismo sistema suele generar conflictos.</p>"},{"location":"introduccion/que-es-docker/#la-solucion-contenedores","title":"La soluci\u00f3n: contenedores","text":"<p>Docker permite encapsular cada experimento en su propio entorno.</p> <p>Ese entorno se describe en un fichero llamado Dockerfile, que define:</p> <ul> <li>qu\u00e9 imagen base se utiliza</li> <li>qu\u00e9 dependencias se instalan</li> <li>qu\u00e9 orden ejecuta el experimento</li> </ul> <p>Cuando se ejecuta el experimento, Docker crea un contenedor con ese entorno.</p> <p>De esta forma:</p> <ul> <li>cada experimento tiene sus propias dependencias</li> <li>no hay conflictos entre proyectos</li> <li>el experimento se puede reproducir f\u00e1cilmente</li> </ul>"},{"location":"introduccion/que-es-docker/#ventajas-de-usar-docker-en-experimentacion","title":"Ventajas de usar Docker en experimentaci\u00f3n","text":"<p>El uso de Docker en experimentos tiene varias ventajas importantes.</p>"},{"location":"introduccion/que-es-docker/#reproducibilidad","title":"Reproducibilidad","text":"<p>El entorno del experimento queda completamente definido.</p> <p>Si otra persona ejecuta el mismo contenedor, obtendr\u00e1 exactamente el mismo entorno.</p>"},{"location":"introduccion/que-es-docker/#aislamiento","title":"Aislamiento","text":"<p>Cada experimento se ejecuta en su propio contenedor, por lo que:</p> <ul> <li>no afecta al sistema</li> <li>no rompe otros experimentos</li> <li>no requiere instalar librer\u00edas globalmente</li> </ul>"},{"location":"introduccion/que-es-docker/#portabilidad","title":"Portabilidad","text":"<p>Un contenedor Docker puede ejecutarse en cualquier sistema que tenga Docker instalado.</p> <p>Esto permite mover experimentos f\u00e1cilmente entre:</p> <ul> <li>ordenadores personales</li> <li>servidores de investigaci\u00f3n</li> <li>infraestructuras de computaci\u00f3n</li> </ul>"},{"location":"introduccion/que-es-docker/#docker-en-este-servidor","title":"Docker en este servidor","text":"<p>En este servidor utilizamos Docker para ejecutar todos los experimentos.</p> <p>Cada experimento se define mediante:</p> <ul> <li>un Dockerfile que describe el entorno</li> <li>un docker-compose.yml que describe c\u00f3mo se ejecuta</li> </ul> <p>Estos ficheros permiten definir completamente el experimento.</p> <p>Sin embargo, a\u00fan queda un problema.</p> <p>Si varios usuarios ejecutan contenedores que utilizan GPU al mismo tiempo, pueden producirse conflictos.</p> <p>Para resolver esto utilizamos gpuq, una herramienta que permite encolar experimentos y ejecutarlos de forma controlada.</p>"},{"location":"introduccion/que-es-gpuq/","title":"\u00bfQu\u00e9 es gpuq?","text":"<p><code>gpuq</code> es una herramienta dise\u00f1ada para gestionar el uso compartido de GPUs en un servidor de investigaci\u00f3n.</p> <p>En muchos grupos de investigaci\u00f3n, varias personas utilizan el mismo servidor para ejecutar experimentos de machine learning. Estos experimentos suelen requerir el uso de GPU para poder entrenar modelos en un tiempo razonable.</p> <p>Cuando varios usuarios comparten el mismo servidor, es habitual encontrarse con problemas como:</p> <ul> <li>varios usuarios intentando utilizar la misma GPU al mismo tiempo</li> <li>experimentos que fallan porque la GPU ya est\u00e1 ocupada</li> <li>dificultad para coordinar qui\u00e9n puede usar la GPU en cada momento</li> </ul> <p><code>gpuq</code> resuelve este problema mediante una cola de trabajos que gestiona la ejecuci\u00f3n de experimentos.</p>"},{"location":"introduccion/que-es-gpuq/#como-funciona-gpuq","title":"C\u00f3mo funciona gpuq","text":"<p>El funcionamiento de <code>gpuq</code> es sencillo.</p> <p>Los usuarios env\u00edan sus experimentos a una cola, en lugar de ejecutarlos directamente en el servidor.</p> <p><code>gpuq</code> se encarga de ejecutar los experimentos uno a uno, garantizando que solo un experimento est\u00e9 utilizando la GPU en cada momento.</p> <p>El flujo de trabajo es el siguiente:</p> <ol> <li>El usuario define su experimento utilizando Docker</li> <li>El usuario env\u00eda el experimento a la cola usando <code>gpuq</code></li> <li><code>gpuq</code> ejecuta los experimentos en orden de llegada</li> </ol> <p>De esta forma se evita que varios experimentos compitan por la misma GPU.</p>"},{"location":"introduccion/que-es-gpuq/#relacion-entre-docker-y-gpuq","title":"Relaci\u00f3n entre Docker y gpuq","text":"<p>En este servidor, los experimentos se ejecutan utilizando contenedores Docker.</p> <p>Esto significa que cada experimento define su entorno de ejecuci\u00f3n mediante:</p> <ul> <li>un Dockerfile, que define el entorno del experimento</li> <li>un docker-compose.yml, que describe c\u00f3mo se ejecuta</li> </ul> <p><code>gpuq</code> utiliza estos ficheros para lanzar el experimento dentro de un contenedor.</p> <p>Esto tiene varias ventajas:</p> <ul> <li>cada experimento se ejecuta en un entorno aislado</li> <li>no hay conflictos entre dependencias</li> <li>los experimentos son reproducibles</li> </ul>"},{"location":"introduccion/que-es-gpuq/#flujo-de-trabajo-completo","title":"Flujo de trabajo completo","text":"<p>El flujo t\u00edpico de trabajo para ejecutar un experimento en el servidor es el siguiente:</p> <ol> <li>Definir el entorno del experimento con un Dockerfile</li> <li>Definir la ejecuci\u00f3n del experimento con docker-compose</li> <li>Enviar el experimento a la cola con gpuq</li> </ol> <p>En las siguientes secciones aprender\u00e1s c\u00f3mo:</p> <ul> <li>escribir un Dockerfile</li> <li>configurar un docker-compose.yml</li> <li>ejecutar experimentos utilizando gpuq</li> </ul>"},{"location":"en/","title":"GPU queue management for experiments \u2014 gpuq","text":"<p><code>gpuq</code> is a tool designed to manage shared GPU usage on a research server.</p> <p>On many servers used by students and research groups, several people run machine learning experiments at the same time. This often leads to problems such as:</p> <ul> <li>multiple users trying to use the same GPU</li> <li>experiments failing because the GPU is already in use</li> <li>difficulty reproducing experiments across different environments</li> </ul> <p><code>gpuq</code> solves these problems by using:</p> <ul> <li>Docker to define the execution environment of experiments</li> <li>docker-compose to describe how each experiment is executed</li> <li>a job queue that guarantees only one experiment uses the GPU at a time</li> </ul>"},{"location":"en/#what-you-will-learn-in-this-documentation","title":"What you will learn in this documentation","text":"<p>This documentation explains step by step how to:</p> <ol> <li>Understand what Docker is and why it is used in experimentation</li> <li>Define an experiment using <code>Dockerfile</code> and <code>docker-compose</code></li> <li>Run experiments on the server using <code>gpuq</code></li> </ol> <p>No previous Docker experience is required.</p>"},{"location":"en/#workflow","title":"Workflow","text":"<p>The typical workflow is the following:</p> <ol> <li>Define the experiment environment using a Dockerfile</li> <li>Define how the experiment runs using docker-compose</li> <li>Submit the experiment to the queue using gpuq</li> </ol>"},{"location":"en/#experiment-structure","title":"Experiment structure","text":"<p>In this documentation we will use a simple structure for experiments:</p> <pre><code>experiment/\n\u251c\u2500\u2500 Dockerfile\n\u251c\u2500\u2500 docker-compose.yml\n\u251c\u2500\u2500 train.py\n\u251c\u2500\u2500 requirements.txt\n\u251c\u2500\u2500 data/\n\u2514\u2500\u2500 results/\n</code></pre> <p>Where:</p> <ul> <li>Dockerfile defines the execution environment</li> <li>docker-compose.yml defines how the experiment runs</li> <li>data/ contains the input data</li> <li>results/ contains the generated results</li> </ul>"},{"location":"en/docker/conceptos-basicos/","title":"Basic Docker Concepts","text":"<p>This section introduces some basic Docker concepts that are necessary to understand how experiments are executed on this server.</p> <p>It is not necessary to know Docker in depth. We will only cover the minimum concepts that will be used in the examples throughout this documentation.</p>"},{"location":"en/docker/conceptos-basicos/#image","title":"Image","text":"<p>A Docker image is a template that contains everything required to run a program.</p> <p>An image can include:</p> <ul> <li>a base operating system</li> <li>system libraries</li> <li>Python dependencies</li> <li>tools required for the experiment</li> </ul> <p>Images are used as the base for creating containers.</p>"},{"location":"en/docker/conceptos-basicos/#container","title":"Container","text":"<p>A container is a running instance of an image.</p> <p>We can think about the relationship between images and containers in a similar way to:</p> <ul> <li>image \u2192 template</li> <li>container \u2192 running program</li> </ul> <p>When we run an experiment with Docker, what actually happens is that Docker creates a container from an image and executes the program inside it.</p> <p>Each container is isolated from the system and from other containers.</p> <p>This allows different experiments to use different dependencies without interfering with each other.</p>"},{"location":"en/docker/conceptos-basicos/#dockerfile","title":"Dockerfile","text":"<p>A Dockerfile is a file that describes how to build a Docker image.</p> <p>It defines aspects such as:</p> <ul> <li>the base image</li> <li>the dependencies that must be installed</li> <li>the working directory</li> <li>the command that runs the experiment</li> </ul> <p>A simple example of a Dockerfile could be:</p> <pre><code>FROM python:3.10\n\nWORKDIR /workspace\n\nCOPY requirements.txt .\nRUN pip install -r requirements.txt\n\nCOPY train.py .\n\nCMD [\"python\", \"train.py\"]\n</code></pre> <p>This file tells Docker how to build the image that will be used to execute the experiment.</p> <p>In the next section we will look in more detail at how to write a Dockerfile.</p>"},{"location":"en/docker/conceptos-basicos/#volumes","title":"Volumes","text":"<p>Volumes allow files to be shared between the host system (the server) and the container.</p> <p>This is especially useful when working with:</p> <ul> <li>datasets</li> <li>experiment results</li> <li>configuration files</li> </ul> <p>For example, we can mount a directory from the server inside the container: <code>./data \u2192 /data</code></p> <p>In this way the container can read the data from <code>/data</code>, and the results written by the container remain on the server.</p> <p>This allows us to clearly separate:</p> <ul> <li>the experiment environment (inside the container)</li> <li>the data and results (outside the container)</li> </ul>"},{"location":"en/docker/conceptos-basicos/#environment-variables","title":"Environment Variables","text":"<p>Environment variables allow parameters to be passed to the experiment when the container is executed.</p> <p>For example, we can define parameters such as:</p> <pre><code>EPOCHS=50\nLEARNING_RATE=0.001\n</code></pre> <p>The program can read these variables to configure its behavior.</p> <p>This allows experiment parameters to be changed without modifying the code.</p>"},{"location":"en/docker/docker-compose/","title":"Using docker-compose","text":"<p>While the Dockerfile defines the experiment environment, the docker-compose.yml file defines how the experiment is executed.</p> <p>It specifies aspects such as:</p> <ul> <li>which image will be used</li> <li>which environment variables are defined</li> <li>which directories are shared with the container</li> <li>whether the experiment uses a GPU</li> </ul> <p>This file completely describes how the experiment should be executed.</p>"},{"location":"en/docker/docker-compose/#example-docker-compose","title":"Example docker-compose","text":"<p>A typical <code>docker-compose.yml</code> file for an experiment might look like this:</p> <pre><code>services:\n\n  experiment:\n\n    build: .\n\n    runtime: nvidia\n    deploy: \n      resources: \n        reservations: \n          devices: \n            - driver: nvidia \n              device_ids: ['0'] \n              capabilities: [gpu]\n\n    environment:\n      - EPOCHS=50\n      - LEARNING_RATE=0.001\n\n    volumes:\n      - ./data:/data:ro\n      - ./results:/results\n</code></pre> <p>This file defines the container that will execute the experiment.</p>"},{"location":"en/docker/docker-compose/#building-the-image","title":"Building the image","text":"<p>The <code>build</code> option indicates that Docker must build the image using the Dockerfile in the current directory.</p> <pre><code>build: .\n</code></pre> <p>This means that before running the experiment, Docker will build the image defined by the Dockerfile.</p>"},{"location":"en/docker/docker-compose/#using-the-gpu","title":"Using the GPU","text":"<p>To allow the container to use the server's GPU, the following configuration is used:</p> <pre><code>runtime: nvidia\ndeploy: \n  resources: \n    reservations: \n      devices: \n        - driver: nvidia \n          device_ids: ['0'] \n          capabilities: [gpu] \n</code></pre> <p>This allows the container to access CUDA and the available GPU.</p>"},{"location":"en/docker/docker-compose/#environment-variables","title":"Environment variables","text":"<p>Environment variables allow experiment parameters to be defined.</p> <pre><code>environment:\n  - EPOCHS=50\n  - LEARNING_RATE=0.001\n</code></pre> <p>These variables will be available inside the container and the program can read them to configure its behavior.</p> <p>This allows the experiment configuration to be changed without modifying the code or rebuilding the Docker image.</p>"},{"location":"en/docker/docker-compose/#volumes","title":"Volumes","text":"<p>Volumes allow directories to be shared between the server and the container.</p> <pre><code>volumes:\n  - ./data:/data:ro\n  - ./results:/results\n</code></pre> <p>In this example:</p> <ul> <li><code>./data</code> contains the input data for the experiment</li> <li><code>./results</code> is used to store the results</li> </ul> <p>Note that the data volume definition includes the <code>:ro</code> tag at the end. This means the volume is mounted in read-only mode. The container can see the data, open it, and operate on it, but it cannot write to that directory. This protects the data from being accidentally modified.</p> <p>In the case of the results directory, this configuration is not included. By default, if nothing is specified, the directory allows both reading and writing.</p> <p>Inside the container these directories will be available as:</p> <pre><code>/data\n/results\n</code></pre> <p>This allows the results generated by the container to remain on the server.</p>"},{"location":"en/docker/dockerfile/","title":"Creating a Dockerfile","text":"<p>A Dockerfile is a file that describes how to build the Docker image that will be used to execute an experiment.</p> <p>This file defines:</p> <ul> <li>the base image</li> <li>the required dependencies</li> <li>the code that will be executed</li> <li>the command that launches the experiment</li> </ul> <p>From the Dockerfile, Docker builds an image that will later be used to create the container where the experiment will run.</p>"},{"location":"en/docker/dockerfile/#basic-structure-of-a-dockerfile","title":"Basic structure of a Dockerfile","text":"<p>A typical Dockerfile for an experiment may look like this:</p> <pre><code>FROM pytorch/pytorch:2.2.0-cuda12.1-cudnn8-runtime\n\nWORKDIR /workspace\n\nCOPY requirements.txt .\nRUN pip install -r requirements.txt\n\nCOPY train.py .\n\nCMD [\"python\", \"train.py\"]\n</code></pre> <p>This file defines the environment required to execute the experiment.</p>"},{"location":"en/docker/dockerfile/#base-image","title":"Base image","text":"<p>The <code>FROM</code> instruction specifies which image will be used as the base.</p> <pre><code>FROM pytorch/pytorch:2.2.0-cuda12.1-cudnn8-runtime\n</code></pre> <p>Base images usually include:</p> <ul> <li>Python</li> <li>machine learning libraries</li> </ul> <p>Choosing a good base image avoids having to install many dependencies manually.</p>"},{"location":"en/docker/dockerfile/#working-directory","title":"Working directory","text":"<p>The <code>WORKDIR</code> instruction defines the directory from which commands will be executed inside the container.</p> <pre><code>WORKDIR /workspace\n</code></pre> <p>From this point onward, files will be copied and commands will be executed in that directory.</p>"},{"location":"en/docker/dockerfile/#installing-dependencies","title":"Installing dependencies","text":"<p>Python dependencies are usually defined in a <code>requirements.txt</code> file.</p> <pre><code>COPY requirements.txt .\nRUN pip install -r requirements.txt\n</code></pre> <p>First the file is copied into the container and then the dependencies are installed.</p> <p>This ensures that the experiment environment is fully defined.</p>"},{"location":"en/docker/dockerfile/#copying-the-code","title":"Copying the code","text":"<p>Next, the experiment code is copied.</p> <pre><code>COPY train.py .\n</code></pre> <p>This makes the file available inside the container.</p>"},{"location":"en/docker/dockerfile/#execution-command","title":"Execution command","text":"<p>The <code>CMD</code> instruction defines which command will be executed when the container starts.</p> <pre><code>CMD [\"python\", \"train.py\"]\n</code></pre> <p>In this case, the container will execute the <code>train.py</code> script.</p>"},{"location":"en/docker/dockerfile/#best-practices","title":"Best practices","text":""},{"location":"en/docker/dockerfile/#avoid-copy","title":"Avoid <code>COPY . .</code>","text":"<p>It is common to find Dockerfiles that contain:</p> <pre><code>COPY . .\n</code></pre> <p>This copies the entire project directory into the container.</p> <p>In experimentation this is usually a bad practice because it may include:</p> <ul> <li>datasets</li> <li>experiment results</li> <li>unnecessary files</li> </ul> <p>It is preferable to copy only the files required to run the experiment.</p>"},{"location":"en/docker/dockerfile/#separate-code-and-data","title":"Separate code and data","text":"<p>Datasets and results should not be copied into the Docker image.</p> <p>Instead, volumes should be used to mount those directories when the container is executed.</p> <p>This allows you to:</p> <ul> <li>reuse the same image for different datasets</li> <li>avoid very large Docker images</li> <li>keep code and data separated</li> </ul> <p>We will see how to do this in the <code>docker-compose</code> section.</p>"},{"location":"en/docker/dockerfile/#use-environment-variables-for-configuration","title":"Use environment variables for configuration","text":"<p>In many experiments it is necessary to change parameters such as:</p> <ul> <li>number of epochs</li> <li>learning rate</li> <li>batch size</li> <li>input or output paths</li> </ul> <p>A common bad practice is modifying the code every time a parameter needs to change and rebuilding the Docker image.</p> <p>This requires rebuilding the image each time, which can be slow and unnecessary.</p> <p>Instead, it is recommended to use environment variables to define these parameters.</p> <p>For example, the experiment code can read values such as:</p> <ul> <li>EPOCHS</li> <li>LEARNING_RATE</li> <li>BATCH_SIZE</li> </ul> <p>These variables can be defined when the container is executed.</p> <p>For example:</p> <pre><code>EPOCHS=50\nLEARNING_RATE=0.001\n</code></pre> <p>In this way:</p> <ul> <li>the experiment code does not change</li> <li>rebuilding the image is not necessary</li> <li>multiple experiments can be launched with different configurations</li> </ul> <p>This allows the same Docker image to be reused for different experiment configurations.</p>"},{"location":"en/experimentos/ejecutar-contenedores/","title":"Running Docker Containers","text":"<p>Once the Dockerfile and the docker-compose file have been defined, the next step is to run the experiment.</p> <p>This section describes the basic Docker commands used to execute and manage containers on the server.</p> <p>It is not necessary to know Docker in depth. We will only cover the most common commands used when working with experiments.</p>"},{"location":"en/experimentos/ejecutar-contenedores/#running-an-experiment","title":"Running an experiment","text":"<p>To run an experiment defined with <code>docker-compose</code>, use the command:</p> <pre><code>docker compose up\n</code></pre> <p>This command performs several actions:</p> <ol> <li>Builds the image defined in the <code>Dockerfile</code> (if it does not already exist).</li> <li>Creates the container.</li> <li>Runs the experiment inside the container.</li> </ol> <p>If the experiment has been executed previously and the image already exists, Docker will reuse that image to start the container.</p>"},{"location":"en/experimentos/ejecutar-contenedores/#selecting-the-docker-compose-file","title":"Selecting the docker-compose file","text":"<p>By default, Docker looks for a file named: <code>docker-compose.yml</code>.</p> <p>If the project uses this name, it is not necessary to specify anything else.</p> <p>However, if the project contains multiple <code>docker-compose</code> files, it is possible to select a specific one using the <code>-f</code> option.</p> <p>For example:</p> <pre><code>docker compose -f docker-compose.test.yml up\n</code></pre> <p>This allows multiple execution configurations within the same project, for example:</p> <ul> <li>one for running the full experiment</li> <li>another for running tests or quick experiments</li> </ul>"},{"location":"en/experimentos/ejecutar-contenedores/#running-in-the-background","title":"Running in the background","text":"<p>Many experiments can take hours or even days.</p> <p>To avoid keeping the terminal open for the entire time, you can use the <code>-d</code> option (detached mode):</p> <pre><code>docker compose up -d\n</code></pre> <p>or, if you want to specify the file:</p> <pre><code>docker compose -f docker-compose.yml up -d\n</code></pre> <p>In this mode, the container runs in the background, and you can close the terminal session and come back later.</p>"},{"location":"en/experimentos/ejecutar-contenedores/#stopping-an-experiment","title":"Stopping an experiment","text":"<p>To stop the containers created by <code>docker-compose</code>, use:</p> <pre><code>docker compose down\n</code></pre> <p>This command:</p> <ul> <li>stops the containers</li> <li>removes the containers created by <code>docker-compose</code></li> </ul> <p>It is important to note that the Docker image is not removed, only the container.</p> <p>This means the experiment can be run again quickly without rebuilding the image.</p>"},{"location":"en/experimentos/ejecutar-contenedores/#viewing-containers","title":"Viewing containers","text":"<p>To see the containers currently running, use:</p> <pre><code>docker container ls\n</code></pre> <p>This command displays information such as:</p> <ul> <li>container identifier</li> <li>image used</li> <li>status</li> <li>container name</li> <li>running time</li> </ul>"},{"location":"en/experimentos/ejecutar-contenedores/#viewing-all-containers","title":"Viewing all containers","text":"<p>By default, only running containers are shown.</p> <p>To see all containers, including those that have already finished, use the <code>-a</code> option:</p> <pre><code>docker container ls -a\n</code></pre> <p>This allows you to check whether an experiment has finished or ended with an error.</p>"},{"location":"en/experimentos/ejecutar-contenedores/#viewing-the-last-container","title":"Viewing the last container","text":"<p>The <code>-l</code> option shows the most recently created container:</p> <pre><code>docker container ls -l\n</code></pre> <p>This is useful when you have just run an experiment and want to quickly locate the corresponding container.</p>"},{"location":"en/experimentos/ejecutar-contenedores/#viewing-container-logs","title":"Viewing container logs","text":"<p>To view the output of a container, use:</p> <pre><code>docker logs &lt;container_id&gt;\n</code></pre> <p>This command displays the standard output of the program running inside the container.</p> <p>In machine learning experiments this usually includes:</p> <ul> <li>training progress</li> <li>metrics</li> <li>error messages</li> <li>debugging information</li> </ul>"},{"location":"en/experimentos/ejecutar-contenedores/#container-identifier","title":"Container identifier","text":"<p>The container identifier is usually a long string, for example <code>8f3a1c2b4d9e7c...</code>. It is not necessary to type the full identifier.</p> <p>Docker allows you to use only the first characters, as long as they are sufficient to uniquely identify the container.</p> <p>For example:</p> <pre><code>docker logs 8f3a\n</code></pre> <p>This will work as long as no other container identifier begins with <code>8f3a</code>.</p>"},{"location":"en/experimentos/ejecutar-contenedores/#managing-docker-images","title":"Managing Docker images","text":"<p>The Docker images built on the system can be listed with:</p> <pre><code>docker image ls\n</code></pre> <p>This command shows:</p> <ul> <li>image name</li> <li>identifier</li> <li>creation date</li> <li>size</li> </ul> <p>Images contain the complete environment required to run the experiment.</p>"},{"location":"en/experimentos/ejecutar-contenedores/#removing-an-image","title":"Removing an image","text":"<p>To remove an image, use:</p> <pre><code>docker image rm &lt;image_id&gt;\n</code></pre> <p>Removing an image is important and necessary when changes are made to the experiment code or environment.</p> <p>If an image already exists, <code>docker compose</code> will reuse it in subsequent runs and will not automatically rebuild the image.</p> <p>This means that changes made to the code may not be reflected in the container.</p> <p>Deleting the image forces Docker to rebuild it the next time the experiment is executed.</p>"},{"location":"en/experimentos/ejecutar-contenedores/#summary","title":"Summary","text":"<p>The most commonly used commands when working with containers are:</p> Command Description <code>docker compose up -d</code> Run an experiment in the background <code>docker compose down</code> Stop and remove containers <code>docker container ls</code> Show running containers <code>docker container ls -a</code> Show all containers <code>docker container ls -l</code> Show the most recently created container <code>docker logs &lt;container&gt;</code> Show the output of a container <code>docker image ls</code> List available images <code>docker image rm &lt;image&gt;</code> Remove an image"},{"location":"en/experimentos/ejemplo-minimo/","title":"Minimal Example","text":"<p>This section shows a minimal example of how to run an experiment using the files described in the previous sections.</p> <p>A typical project may have a structure like the following:</p> <pre><code>experiment/\n\u251c\u2500\u2500 Dockerfile\n\u251c\u2500\u2500 docker-compose.yml\n\u251c\u2500\u2500 docker-compose.test.yml\n\u251c\u2500\u2500 requirements.txt\n\u251c\u2500\u2500 main.py\n\u251c\u2500\u2500 model.py\n\u251c\u2500\u2500 test.py\n\u251c\u2500\u2500 data/\n\u2514\u2500\u2500 results/\n</code></pre> <p>The experiment can be executed in two different ways depending on whether it requires a GPU or not.</p> <ul> <li>Experiments without GPU \u2192 run directly with <code>docker compose</code></li> <li>Experiments with GPU \u2192 submit to the queue with <code>gpuq</code></li> </ul>"},{"location":"en/experimentos/ejemplo-minimo/#running-an-experiment-without-gpu","title":"Running an experiment without GPU","text":"<p>If the experiment does not require a GPU, it can be executed directly with Docker.</p> <p>For example, to run the experiment defined in <code>docker-compose.yml</code>:</p> <pre><code>docker compose up -d\n</code></pre> <p>This command:</p> <ol> <li>builds the image defined in the <code>Dockerfile</code> (if it does not exist)</li> <li>creates the container</li> <li>runs the experiment in the background</li> </ol>"},{"location":"en/experimentos/ejemplo-minimo/#running-an-experiment-with-gpu","title":"Running an experiment with GPU","text":"<p>Experiments that use a GPU must not be executed directly with Docker.</p> <p>Instead, they must be submitted to the queue using gpuq.</p> <p>This guarantees that:</p> <ul> <li>only one experiment uses the GPU at a time</li> <li>experiments are executed in order</li> <li>a persistent record of executed jobs is maintained</li> </ul>"},{"location":"en/experimentos/ejemplo-minimo/#submitting-a-job-to-the-queue","title":"Submitting a job to the queue","text":"<p>To submit an experiment to the queue, use the command:</p> <pre><code>gpuq submit --project PATH\n</code></pre> <p>where <code>PATH</code> is the directory containing the project.</p> <p>For example:</p> <pre><code>gpuq submit --project ./experiment-name\n</code></pre> <p>This command:</p> <ul> <li>registers the job in the queue</li> <li>generates a unique identifier for the job</li> <li>associates the job with the user who submitted it</li> </ul> <p>The job remains in the queued state until the system executes it.</p>"},{"location":"en/experimentos/ejemplo-minimo/#selecting-the-docker-compose-file","title":"Selecting the docker-compose file","text":"<p>By default, <code>gpuq</code> will use the file <code>docker-compose.yml</code>.</p> <p>If you want to use another <code>docker-compose</code> file, you can specify it using the <code>--compose</code> option.</p> <p>For example:</p> <pre><code>gpuq submit --project ./experiment --compose docker-compose.test.yml\n</code></pre>"},{"location":"en/experimentos/ejemplo-minimo/#listing-jobs-in-the-queue","title":"Listing jobs in the queue","text":"<p>To see the jobs registered in the queue, use:</p> <pre><code>gpuq list\n</code></pre> <p>This command shows information such as:</p> <ul> <li>job identifier</li> <li>user who submitted it</li> <li>creation date</li> <li>description</li> </ul>"},{"location":"en/experimentos/ejemplo-minimo/#cancelling-a-job","title":"Cancelling a job","text":"<p>To cancel a job, use:</p> <pre><code>gpuq cancel JOB_ID\n</code></pre> <p>For example:</p> <pre><code>gpuq cancel job-1a2b3c4d\n</code></pre> <p>If the job is in the queued state, it will be moved to the canceled state.</p> <p>If the job is in the running state, the system will mark the job as canceled and the execution process will take care of stopping it.</p>"},{"location":"en/experimentos/estructura-experimento/","title":"Experiment Structure","text":"<p>This section describes a simple and recommended structure for organizing an experiment that will be executed on the server.</p> <p>The idea is to clearly separate three different elements:</p> <ul> <li>the execution environment</li> <li>the experiment code</li> <li>the data and results</li> </ul> <p>This separation makes it easier to:</p> <ul> <li>reproduce experiments</li> <li>change configurations easily</li> <li>avoid unnecessary rebuilding of Docker images</li> </ul>"},{"location":"en/experimentos/estructura-experimento/#recommended-structure","title":"Recommended structure","text":"<p>A typical experiment may have a structure like the following:</p> <pre><code>experiment/\n\u251c\u2500\u2500 Dockerfile\n\u251c\u2500\u2500 docker-compose.yml\n\u251c\u2500\u2500 docker-compose.test.yml\n\u251c\u2500\u2500 requirements.txt\n\u251c\u2500\u2500 main.py\n\u251c\u2500\u2500 model.py\n\u251c\u2500\u2500 test.py\n\u251c\u2500\u2500 data/\n\u2514\u2500\u2500 results/\n</code></pre> <p>Where:</p> File or directory Description <code>Dockerfile</code> Defines the execution environment of the experiment <code>docker-compose.yml</code> Defines how the main experiment is executed <code>docker-compose.test.yml</code> Variant used to run tests <code>requirements.txt</code> Python dependencies <code>main.py</code> Main script of the experiment <code>model.py</code> Model definition <code>test.py</code> Script for tests or quick experiments <code>data/</code> Input data <code>results/</code> Results generated by the experiment"},{"location":"en/experimentos/estructura-experimento/#dockerfile","title":"Dockerfile","text":"<p>The Dockerfile defines the environment in which the experiment will run.</p> <p>This includes:</p> <ul> <li>the base image</li> <li>the dependencies</li> <li>the required files</li> <li>the default command</li> </ul> <p> Download Dockerfile</p> <pre><code>FROM python:3.11\nENV PYTHONUNBUFFERED=1\n\nWORKDIR /workspace\n\nCOPY requirements.txt .\nRUN pip install --no-cache-dir -r requirements.txt\n\nCOPY main.py .\nCOPY model.py .\nCOPY test.py .\n\nCMD [\"python\", \"main.py\"]\n</code></pre> <p>This file is used to build the Docker image that will run the experiment.</p>"},{"location":"en/experimentos/estructura-experimento/#docker-compose","title":"docker-compose","text":"<p>The <code>docker-compose</code> file defines how the experiment is executed.</p> <p>It specifies aspects such as:</p> <ul> <li>how the image should be built</li> <li>which environment variables to use</li> <li>which directories should be mounted as volumes</li> <li>whether the experiment uses a GPU</li> </ul>"},{"location":"en/experimentos/estructura-experimento/#running-the-main-experiment","title":"Running the main experiment","text":"<p>This file runs the main experiment using <code>main.py</code>.</p> <p> Download docker-compose.yml</p> <pre><code>services:\n  experiment:\n    build: .\n    runtime: nvidia\n    deploy:\n      resources:\n        reservations:\n          devices:\n            - driver: nvidia\n              device_ids: ['0']\n              capabilities: [gpu]\n    environment:\n      - BATCH_SIZE=32\n    volumes:\n      - ./data:/data:ro\n      - ./results:/results\n</code></pre>"},{"location":"en/experimentos/estructura-experimento/#running-tests","title":"Running tests","text":"<p>Sometimes it is useful to run a variant of the experiment, for example to:</p> <ul> <li>test changes quickly</li> <li>run tests</li> <li>validate that the environment works correctly</li> </ul> <p>For this purpose we can create a second <code>docker-compose</code> file.</p> <p> Download docker-compose.test.yml</p> <pre><code>services:\n  experiment:\n    build: .\n    runtime: nvidia\n    deploy:\n      resources:\n        reservations:\n          devices:\n            - driver: nvidia\n              device_ids: ['0']\n              capabilities: [gpu]\n    environment:\n      - EPOCHS=50\n      - LEARNING_RATE=0.001\n      - BATCH_SIZE=32\n    volumes:\n      - ./data:/data:ro\n      - ./results:/results\n    command: [\"python\", \"test.py\"]\n</code></pre> <p>This file can modify aspects such as:</p> <ul> <li>the script that is executed. IMPORTANT: Notice how the <code>command</code> field is used to change the script that runs when the experiment starts</li> <li>the experiment parameters</li> <li>specific configurations for testing</li> </ul>"},{"location":"en/gpuq/ejecutar-trabajos/","title":"Running jobs with gpuq","text":"<p><code>gpuq</code> is a tool designed to submit GPU-based experiments to an execution queue on a shared server.</p> <p>Instead of running a container directly with Docker, experiments that require a GPU must be submitted to the queue using <code>gpuq</code>. This makes it possible to control GPU usage and prevent multiple experiments from attempting to use it at the same time.</p> <p>Each job submitted to the queue is recorded persistently in the filesystem.</p>"},{"location":"en/gpuq/ejecutar-trabajos/#submitting-a-job-to-the-queue","title":"Submitting a job to the queue","text":"<p>To submit an experiment to the queue, use the command:</p> <pre><code>gpuq submit --project PATH\n</code></pre> <p>where <code>PATH</code> is the directory that contains the project.</p> <p>For example:</p> <pre><code>gpuq submit --project ./experiment\n</code></pre> <p>The project directory must contain at least one <code>docker-compose</code> file that describes how to execute the experiment.</p> <p>When this command is executed:</p> <ul> <li>a unique identifier is generated for the job</li> <li>the job is associated with the user who submitted it</li> <li>the creation timestamp is recorded</li> <li>the job is stored in the queue with state queued</li> </ul> <p>The job will remain in that state until the system responsible for executing the queue starts it.</p>"},{"location":"en/gpuq/ejecutar-trabajos/#selecting-the-docker-compose-file","title":"Selecting the docker-compose file","text":"<p>By default, <code>gpuq</code> will use the <code>docker-compose.yml</code> file. If the project contains multiple <code>docker-compose</code> files, it is possible to select a specific one using the <code>--compose</code> option.</p> <p>For example:</p> <pre><code>gpuq submit --project ./experiment --compose docker-compose.test.yml\n</code></pre> <p>This allows different configurations to be used within the same project.</p> <p>For example:</p> <ul> <li>run the main experiment</li> <li>run quick tests</li> <li>run alternative configurations</li> </ul>"},{"location":"en/gpuq/ejecutar-trabajos/#adding-a-description-to-the-job","title":"Adding a description to the job","text":"<p>It is possible to add an optional description to the job using the <code>--description</code> option.</p> <p>For example:</p> <pre><code>gpuq submit --project ./experiment --description \"baseline training\"\n</code></pre> <p>The description is free text that helps identify the purpose of the experiment.</p>"},{"location":"en/gpuq/ejecutar-trabajos/#what-happens-when-a-job-is-submitted","title":"What happens when a job is submitted","text":"<p>When <code>gpuq submit</code> is executed, the tool does not run the experiment directly.</p> <p>Instead, it:</p> <ol> <li>validates that the project directory exists</li> <li>registers the job in the queue</li> <li>assigns a unique identifier to the job</li> <li>stores the job metadata in the filesystem</li> </ol> <p>The job is registered with state <code>queued</code>. An independent system component will later execute the job.</p>"},{"location":"en/gpuq/ejecutar-trabajos/#job-identifier","title":"Job identifier","text":"<p>Each job receives a unique identifier with a format similar to <code>job-1a2b3c4d</code>. This identifier is used to:</p> <ul> <li>check the job status</li> <li>cancel the job</li> <li>identify the experiment in the queue</li> </ul>"},{"location":"en/gpuq/ejecutar-trabajos/#what-gpuq-does-and-what-it-does-not-do","title":"What gpuq does and what it does not do","text":"<p>It is important to understand which responsibilities belong to <code>gpuq</code> and which do not.</p> <p><code>gpuq</code> is responsible for:</p> <ul> <li>registering jobs in a persistent queue</li> <li>assigning unique identifiers to jobs</li> <li>maintaining the state of each job</li> <li>allowing jobs to be listed and canceled</li> </ul> <p>However, <code>gpuq</code> does not execute containers directly. The actual execution is performed by a separate system component.</p> <p>In addition, <code>gpuq</code> does not remove Docker containers or images.</p> <p>When a job finishes, the containers created by Docker remain in the system. Similarly, the Docker images built to execute the experiments are not automatically removed.</p> <p>Cleaning up containers or images that are no longer needed remains the responsibility of the user.</p> <p>The commands required to manage containers and images are described in the Docker section.</p>"},{"location":"en/gpuq/ejecutar-trabajos/#summary","title":"Summary","text":"<p>The main commands used to submit jobs to the queue with <code>gpuq</code> are:</p> Command Description <code>gpuq submit --project ./experiment</code> Submit an experiment to the queue using the project's <code>docker-compose.yml</code>. <code>gpuq submit --project ./experiment --compose docker-compose.test.yml</code> Submit the experiment using a specific <code>docker-compose</code> file. <code>gpuq submit --project ./experiment --description \"baseline training\"</code> Submit an experiment with a description to identify the job. <p>In all cases the command registers the job in the queue, generates a unique identifier, and leaves the job in the <code>queued</code> state until the system responsible for executing the queue starts it.</p> <p>In the next section we will see how to monitor jobs in the queue and check their status.</p>"},{"location":"en/gpuq/logs-y-resultados/","title":"Logs and results","text":"<p>Once a job has been submitted to the queue and begins execution, it is common to want to check:</p> <ul> <li>the experiment output</li> <li>possible errors during execution</li> <li>the generated results</li> </ul> <p>This section explains how to access this information.</p>"},{"location":"en/gpuq/logs-y-resultados/#experiment-output","title":"Experiment output","text":"<p>Experiments submitted through <code>gpuq</code> run inside Docker containers using the configuration defined in the <code>docker-compose</code> file.</p> <p>For this reason, the experiment output can be inspected using standard Docker tools.</p>"},{"location":"en/gpuq/logs-y-resultados/#viewing-container-logs","title":"Viewing container logs","text":"<p>To view the output of a container, use the command:</p> <pre><code>docker logs CONTAINER_ID\n</code></pre> <p>This command displays the standard output of the program running inside the container.</p> <p>In machine learning experiments this usually includes:</p> <ul> <li>training progress</li> <li>model metrics</li> <li>debugging messages</li> <li>execution errors</li> </ul>"},{"location":"en/gpuq/logs-y-resultados/#locating-the-container","title":"Locating the container","text":"<p>To find the container associated with an experiment you can use:</p> <pre><code>docker container ls -a\n</code></pre>"},{"location":"en/gpuq/logs-y-resultados/#experiment-results","title":"Experiment results","text":"<p>In the recommended project structure, a directory called <code>results</code> is used. This directory is mounted inside the container through a volume defined in <code>docker-compose</code>.</p> <p>This means that any file written by the experiment to <code>/results</code> inside the container will automatically appear in the project's <code>results/</code> directory.</p> <p>This ensures that:</p> <ul> <li>results remain on the system even after the container finishes</li> <li>results can be analyzed without needing to access the container</li> </ul>"},{"location":"en/gpuq/logs-y-resultados/#debugging-errors","title":"Debugging errors","text":"<p>If an experiment fails, the typical steps to investigate the problem are:</p> <ol> <li>check the job status with <code>gpuq list</code></li> <li>locate the corresponding container</li> <li>inspect the container logs with <code>docker logs</code></li> </ol> <p>Error messages from the program usually appear in the container logs.</p>"},{"location":"en/gpuq/logs-y-resultados/#cleaning-up-containers-and-images","title":"Cleaning up containers and images","text":"<p>It is important to remember that <code>gpuq</code> does not remove Docker containers or images.</p> <p>When an experiment finishes:</p> <ul> <li>the container created by Docker remains on the system</li> <li>the Docker image used to run it also remains</li> </ul> <p>Removing containers or images that are no longer needed is the responsibility of the user.</p> <p>The commands required to manage these resources are described in the Docker section.</p>"},{"location":"en/gpuq/logs-y-resultados/#summary","title":"Summary","text":"<p>The main tools used to inspect the execution of an experiment are:</p> Command Description <code>gpuq list</code> Show the status of jobs in the queue <code>docker container ls</code> Show running containers <code>docker container ls -a</code> Show all containers <code>docker logs CONTAINER_ID</code> View the output of a container <p>Experiment results are stored in the <code>results/</code> directory of the project.</p>"},{"location":"en/gpuq/monitorizar-trabajos/","title":"Monitoring jobs","text":"<p>Once a job has been submitted to the queue with <code>gpuq</code>, it is possible to check its status and manage its execution using monitoring commands.</p> <p>This section describes the available commands to:</p> <ul> <li>list jobs in the queue</li> <li>check their status</li> <li>cancel jobs if necessary</li> </ul>"},{"location":"en/gpuq/monitorizar-trabajos/#listing-jobs","title":"Listing jobs","text":"<p>To see the jobs registered in the queue, use:</p> <pre><code>gpuq list\n</code></pre> <p>This command shows all jobs known to the queue, regardless of their state.</p> <p>For each job, information such as the following is displayed:</p> <ul> <li>job identifier</li> <li>user who submitted it</li> <li>creation date</li> <li>description (if present)</li> </ul> <p>This command does not modify the system state. It only queries the information stored in the queue.</p>"},{"location":"en/gpuq/monitorizar-trabajos/#job-states","title":"Job states","text":"<p>Each job in the queue is always in one of the following states:</p> State Description <code>queued</code> The job is registered in the queue and is waiting to be executed. <code>running</code> The job is currently running. <code>finished</code> The job finished successfully. <code>failed</code> The job finished with an error. <code>canceled</code> The job was canceled by the user. <p>The state of a job is determined by the location of its file within the queue directory structure in the filesystem.</p>"},{"location":"en/gpuq/monitorizar-trabajos/#filtering-jobs-by-state","title":"Filtering jobs by state","text":"<p>It is possible to display only the jobs that are in a specific state using the <code>--state</code> option.</p> <p>For example, to see only jobs waiting in the queue:</p> <pre><code>gpuq list --state queued\n</code></pre> <p>Other states can also be queried, for example:</p> <pre><code>gpuq list --state running\n</code></pre> <p>This is useful when there are many jobs registered in the system.</p>"},{"location":"en/gpuq/monitorizar-trabajos/#canceling-a-job","title":"Canceling a job","text":"<p>To cancel a job, use the command:</p> <pre><code>gpuq cancel JOB_ID\n</code></pre> <p>For example:</p> <pre><code>gpuq cancel job-1a2b3c4d\n</code></pre>"},{"location":"en/gpuq/monitorizar-trabajos/#canceling-jobs-in-the-queued-state","title":"Canceling jobs in the queued state","text":"<p>If the job is still in the <code>queued</code> state, the command will move it to the <code>canceled</code> state. This means the job will not be executed.</p>"},{"location":"en/gpuq/monitorizar-trabajos/#canceling-jobs-in-the-running-state","title":"Canceling jobs in the running state","text":"<p>If the job is already running (<code>running</code>), the command will mark the job as canceled.</p> <p>The actual termination of the execution process will be handled by the system component responsible for running the jobs.</p>"},{"location":"en/gpuq/monitorizar-trabajos/#canceling-finished-jobs","title":"Canceling finished jobs","text":"<p>Attempting to cancel a job that is already in the following states:</p> <ul> <li><code>finished</code></li> <li><code>failed</code></li> <li><code>canceled</code></li> </ul> <p>will result in an error, since those jobs can no longer be modified.</p>"},{"location":"en/gpuq/monitorizar-trabajos/#job-lifecycle","title":"Job lifecycle","text":"<p>A job submitted to the queue typically follows the following lifecycle:</p> <pre><code>stateDiagram-v2\n    [*] --&gt; queued : submit\n    queued --&gt; running : dispatcher starts job\n    queued --&gt; canceled : cancel\n    running --&gt; finished : success\n    running --&gt; failed : error\n    running --&gt; canceled : cancel\n</code></pre> <p>This cycle reflects the possible state transitions during the execution of an experiment.</p>"},{"location":"en/gpuq/monitorizar-trabajos/#summary","title":"Summary","text":"<p>The main commands used to monitor jobs are:</p> Command Description <code>gpuq list</code> Show all jobs registered in the queue. <code>gpuq list --state queued</code> Show only jobs waiting in the queue. <code>gpuq list --state running</code> Show jobs currently running. <code>gpuq cancel JOB_ID</code> Cancel a job identified by its ID. <p>In the next section we will see how to inspect experiment logs and access the results generated by jobs.</p>"},{"location":"en/introduccion/que-es-docker/","title":"What is Docker?","text":"<p>Docker is a tool that allows programs to run inside isolated environments called containers.</p> <p>A container includes everything required to run a program:</p> <ul> <li>the base operating system</li> <li>the required libraries</li> <li>Python dependencies</li> <li>the experiment code itself</li> </ul> <p>This allows an experiment to be executed always in the same environment, regardless of the system where it runs.</p>"},{"location":"en/introduccion/que-es-docker/#the-problem-it-tries-to-solve","title":"The problem it tries to solve","text":"<p>In machine learning experimentation it is very common to encounter problems such as:</p> <ul> <li>an experiment works on your computer but fails on the server</li> <li>different versions of Python or a library cause errors</li> <li>installing dependencies breaks other projects</li> <li>different users require different environments</li> </ul> <p>For example, one experiment may require Python 3.10 and PyTorch 2.1, while another may need Python 3.8 and TensorFlow 2.10.</p> <p>Installing all of this in the same system often creates conflicts.</p>"},{"location":"en/introduccion/que-es-docker/#the-solution-containers","title":"The solution: containers","text":"<p>Docker allows each experiment to be encapsulated in its own environment.</p> <p>This environment is described in a file called a Dockerfile, which defines:</p> <ul> <li>which base image is used</li> <li>which dependencies are installed</li> <li>which command runs the experiment</li> </ul> <p>When the experiment is executed, Docker creates a container with that environment.</p> <p>This means that:</p> <ul> <li>each experiment has its own dependencies</li> <li>there are no conflicts between projects</li> <li>the experiment can be easily reproduced</li> </ul>"},{"location":"en/introduccion/que-es-docker/#advantages-of-using-docker-in-experimentation","title":"Advantages of using Docker in experimentation","text":"<p>Using Docker for experiments provides several important advantages.</p>"},{"location":"en/introduccion/que-es-docker/#reproducibility","title":"Reproducibility","text":"<p>The experiment environment is fully defined.</p> <p>If someone else runs the same container, they will obtain exactly the same environment.</p>"},{"location":"en/introduccion/que-es-docker/#isolation","title":"Isolation","text":"<p>Each experiment runs in its own container, which means:</p> <ul> <li>it does not affect the system</li> <li>it does not break other experiments</li> <li>it does not require installing libraries globally</li> </ul>"},{"location":"en/introduccion/que-es-docker/#portability","title":"Portability","text":"<p>A Docker container can run on any system where Docker is installed.</p> <p>This allows experiments to be easily moved between:</p> <ul> <li>personal computers</li> <li>research servers</li> <li>computing infrastructures</li> </ul>"},{"location":"en/introduccion/que-es-docker/#docker-on-this-server","title":"Docker on this server","text":"<p>On this server we use Docker to run all experiments.</p> <p>Each experiment is defined using:</p> <ul> <li>a Dockerfile that describes the environment</li> <li>a docker-compose.yml that describes how it runs</li> </ul> <p>These files allow the experiment to be fully defined.</p> <p>However, there is still one problem.</p> <p>If multiple users run containers that use the GPU at the same time, conflicts may occur.</p> <p>To solve this we use gpuq, a tool that allows experiments to be queued and executed in a controlled way.</p>"},{"location":"en/introduccion/que-es-gpuq/","title":"What is gpuq?","text":"<p><code>gpuq</code> is a tool designed to manage shared GPU usage on a research server.</p> <p>In many research groups, several people use the same server to run machine learning experiments. These experiments usually require GPU usage in order to train models in a reasonable amount of time.</p> <p>When multiple users share the same server, it is common to encounter problems such as:</p> <ul> <li>several users trying to use the same GPU at the same time</li> <li>experiments failing because the GPU is already in use</li> <li>difficulty coordinating who can use the GPU at any given time</li> </ul> <p><code>gpuq</code> solves this problem by using a job queue that manages the execution of experiments.</p>"},{"location":"en/introduccion/que-es-gpuq/#how-gpuq-works","title":"How gpuq works","text":"<p>The way <code>gpuq</code> works is simple.</p> <p>Users submit their experiments to a queue, instead of executing them directly on the server.</p> <p><code>gpuq</code> runs the experiments one at a time, ensuring that only one experiment is using the GPU at any given moment.</p> <p>The workflow is as follows:</p> <ol> <li>The user defines their experiment using Docker</li> <li>The user submits the experiment to the queue using <code>gpuq</code></li> <li><code>gpuq</code> executes the experiments in the order they were submitted</li> </ol> <p>This prevents multiple experiments from competing for the same GPU.</p>"},{"location":"en/introduccion/que-es-gpuq/#relationship-between-docker-and-gpuq","title":"Relationship between Docker and gpuq","text":"<p>On this server, experiments are executed using Docker containers.</p> <p>This means that each experiment defines its execution environment using:</p> <ul> <li>a Dockerfile, which defines the experiment environment</li> <li>a docker-compose.yml, which describes how the experiment is executed</li> </ul> <p><code>gpuq</code> uses these files to launch the experiment inside a container.</p> <p>This provides several advantages:</p> <ul> <li>each experiment runs in an isolated environment</li> <li>there are no dependency conflicts</li> <li>experiments are reproducible</li> </ul>"},{"location":"en/introduccion/que-es-gpuq/#complete-workflow","title":"Complete workflow","text":"<p>The typical workflow for running an experiment on the server is the following:</p> <ol> <li>Define the experiment environment with a Dockerfile</li> <li>Define how the experiment runs with docker-compose</li> <li>Submit the experiment to the queue using gpuq</li> </ol> <p>In the following sections you will learn how to:</p> <ul> <li>write a Dockerfile</li> <li>configure a docker-compose.yml</li> <li>run experiments using gpuq</li> </ul>"}]}